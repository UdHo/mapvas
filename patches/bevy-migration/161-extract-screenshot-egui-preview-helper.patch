diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 09b6302..b6c698b 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -162,6 +162,7 @@ Apply these patches in order from the repository root:
 158. `158-use-rect-for-egui-attribution.patch`
 159. `159-remove-timeline-layer-trait-impl.patch`
 160. `160-extract-command-egui-draw-helper.patch`
+161. `161-extract-screenshot-egui-preview-helper.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/screenshot.rs b/src/map/mapvas_egui/layer/screenshot.rs
index d8ad76c..b2e872c 100644
--- a/src/map/mapvas_egui/layer/screenshot.rs
+++ b/src/map/mapvas_egui/layer/screenshot.rs
@@ -106,32 +106,13 @@ impl ScreenshotLayer {
   fn compute_gamma(&self) -> f32 {
     1. - (self.last_screenshot_time.elapsed().as_millis() as f32) / 10000.
   }
-}
-
-const NAME: &str = "Screenshot Layer";
-
-impl Layer for ScreenshotLayer {
-  fn process_pending_events(&mut self) {
-    for event in self.receiver.try_iter() {
-      if let MapEvent::Screenshot(path) = event {
-        self.take_screenshot(path);
-      }
-    }
-    self.handle_screenshots();
-  }
 
-  fn draw_egui(
-    &mut self,
-    ui: &mut egui::Ui,
-    _transform: &crate::map::coordinates::Transform,
-    frame: EguiMapFrame,
-  ) {
+  fn draw_screenshot_preview(&mut self, ui: &mut egui::Ui, rect: Rect) {
     if !self.visible() {
       return;
     }
 
     if let Some(texture) = &self.last_screenshot {
-      let rect = frame.rect;
       let screenshot_rect = rect
         .with_min_x(rect.max.x - (rect.max.x - rect.min.x) * 0.2)
         .with_min_y(rect.max.y - (rect.max.y - rect.min.y) * 0.2);
@@ -155,6 +136,28 @@ impl Layer for ScreenshotLayer {
       self.ctx.request_repaint_after_secs(0.2);
     }
   }
+}
+
+const NAME: &str = "Screenshot Layer";
+
+impl Layer for ScreenshotLayer {
+  fn process_pending_events(&mut self) {
+    for event in self.receiver.try_iter() {
+      if let MapEvent::Screenshot(path) = event {
+        self.take_screenshot(path);
+      }
+    }
+    self.handle_screenshots();
+  }
+
+  fn draw_egui(
+    &mut self,
+    ui: &mut egui::Ui,
+    _transform: &crate::map::coordinates::Transform,
+    frame: EguiMapFrame,
+  ) {
+    self.draw_screenshot_preview(ui, frame.rect);
+  }
 
   fn name(&self) -> &str {
     NAME
