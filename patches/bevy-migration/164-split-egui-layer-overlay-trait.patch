diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 7bef588..2a08d8b 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -165,6 +165,7 @@ Apply these patches in order from the repository root:
 161. `161-extract-screenshot-egui-preview-helper.patch`
 162. `162-extract-tile-egui-draw-helper.patch`
 163. `163-extract-shape-egui-overlay-helper.patch`
+164. `164-split-egui-layer-overlay-trait.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/commands.rs b/src/map/mapvas_egui/layer/commands.rs
index 3f6672d..e6bc491 100644
--- a/src/map/mapvas_egui/layer/commands.rs
+++ b/src/map/mapvas_egui/layer/commands.rs
@@ -13,7 +13,7 @@ use crate::map::{
   geometry_collection::Geometry,
 };
 
-use super::{EguiMapFrame, GeometrySnapshot, Layer, LayerProperties};
+use super::{EguiLayer, EguiMapFrame, GeometrySnapshot, Layer, LayerProperties};
 
 mod egui_geometry;
 mod external_cmd;
@@ -463,6 +463,8 @@ impl Layer for CommandLayer {
   }
 }
 
+impl EguiLayer for CommandLayer {}
+
 impl CommandLayer {
   fn calculate_distance_to_command(
     command: &dyn Command,
diff --git a/src/map/mapvas_egui/layer/screenshot.rs b/src/map/mapvas_egui/layer/screenshot.rs
index b2e872c..80c2c2b 100644
--- a/src/map/mapvas_egui/layer/screenshot.rs
+++ b/src/map/mapvas_egui/layer/screenshot.rs
@@ -6,7 +6,7 @@ use egui::{
 
 use crate::map::{map_event::MapEvent, mapvas_egui::helpers::current_time_screenshot_name};
 
-use super::{EguiMapFrame, Layer, LayerProperties};
+use super::{EguiLayer, EguiMapFrame, Layer, LayerProperties};
 
 /// A layer that takes screenshots of the map, saves the image, and displays it on the map.
 pub struct ScreenshotLayer {
@@ -173,3 +173,5 @@ impl Layer for ScreenshotLayer {
 
   fn ui_egui_content(&mut self, _ui: &mut egui::Ui) {}
 }
+
+impl EguiLayer for ScreenshotLayer {}
diff --git a/src/map/mapvas_egui/layer/shape_layer.rs b/src/map/mapvas_egui/layer/shape_layer.rs
index d468602..be8653f 100644
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -1,6 +1,6 @@
 use super::super::helpers::{pixel_to_pos, pos_to_pixel};
 use super::{
-  EguiMapFrame, GeometrySnapshot, Layer, LayerProperties, Searchable, SubLayerInfo,
+  EguiLayer, EguiMapFrame, GeometrySnapshot, Layer, LayerProperties, Searchable, SubLayerInfo,
   geometry_highlighting::GeometryHighlighter, geometry_rasterizer,
 };
 use rstar::{AABB, RTree, RTreeObject};
@@ -744,6 +744,12 @@ impl ShapeLayer {
 
 const NAME: &str = "Shape Layer";
 
+impl EguiLayer for ShapeLayer {
+  fn draw_egui_overlay(&mut self, ui: &mut Ui, transform: &Transform) {
+    self.draw_egui_overlay_content(ui, transform);
+  }
+}
+
 impl Layer for ShapeLayer {
   /// Get temporal range from all geometries in this layer
   fn get_temporal_range(
@@ -785,10 +791,6 @@ impl Layer for ShapeLayer {
     self.update_hover_highlighting(pos, transform);
   }
 
-  fn draw_egui_overlay(&mut self, ui: &mut Ui, transform: &Transform) {
-    self.draw_egui_overlay_content(ui, transform);
-  }
-
   #[allow(clippy::too_many_lines)]
   fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
     profile_scope!("ShapeLayer::draw_egui");
diff --git a/src/map/mapvas_egui/layer/tile_layer.rs b/src/map/mapvas_egui/layer/tile_layer.rs
index 1877e47..4427953 100644
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -22,7 +22,7 @@ use crate::{
   task_tracker::{TaskCategory, TaskGuard},
 };
 
-use super::{EguiMapFrame, Layer, LayerProperties};
+use super::{EguiLayer, EguiMapFrame, Layer, LayerProperties};
 use crate::map::mapvas_egui::helpers::pixel_to_pos;
 
 /// Compute the pixel x-coordinate of the viewport's left edge in internal canvas coordinates.
@@ -993,7 +993,9 @@ impl Layer for TileLayer {
   fn set_headless(&mut self) {
     self.preload_enabled = false;
   }
+}
 
+impl EguiLayer for TileLayer {
   fn draw_egui_attribution(&self, ui: &mut Ui, clip_rect: Rect) {
     if self.coordinate_display_mode != CoordinateDisplayMode::GridOnly
       && !self.loaded_tiles.is_empty()
diff --git a/src/map/mapvas_egui/layer.rs b/src/map/mapvas_egui/layer.rs
index 29176ec..c8308fa 100644
--- a/src/map/mapvas_egui/layer.rs
+++ b/src/map/mapvas_egui/layer.rs
@@ -61,7 +61,6 @@ impl EguiMapFrame {
 /// E.g. a layer to draw the map tiles and one to draw the shapes.
 pub trait Layer {
   fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame);
-  fn draw_egui_overlay(&mut self, _ui: &mut Ui, _transform: &Transform) {}
   fn handle_hover(&mut self, _pos: Option<PixelPosition>, _transform: &Transform) {}
   fn clear(&mut self) {}
   fn name(&self) -> &str;
@@ -142,9 +141,6 @@ pub trait Layer {
   /// Configure this layer for headless rendering (disable preloading, etc.).
   fn set_headless(&mut self) {}
 
-  /// Draw egui attribution or licensing overlays after all map content is painted.
-  fn draw_egui_attribution(&self, _ui: &mut Ui, _clip_rect: Rect) {}
-
   /// Return info about named sub-layers (e.g. each id sent via `mapcat`).
   fn sub_layers(&self) -> Vec<SubLayerInfo> {
     vec![]
@@ -194,6 +190,14 @@ pub trait Layer {
   }
 }
 
+/// Egui-specific layer hooks used by the legacy egui map renderer and egui overlays.
+pub trait EguiLayer: Layer {
+  fn draw_egui_overlay(&mut self, _ui: &mut Ui, _transform: &Transform) {}
+
+  /// Draw egui attribution or licensing overlays after all map content is painted.
+  fn draw_egui_attribution(&self, _ui: &mut Ui, _clip_rect: Rect) {}
+}
+
 /// Capability for layers that support text search and filtering of their contents.
 pub trait Searchable {
   fn search_geometries(&mut self, query: &str);
diff --git a/src/map/mapvas_egui.rs b/src/map/mapvas_egui.rs
index f9672b7..d257e97 100644
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -21,7 +21,7 @@ use crate::{
 use arboard::Clipboard;
 use egui::{InputState, PointerButton, Rect, Response, Sense, Ui};
 use helpers::{pixel_to_pos, point_to_coordinate, pos_to_pixel, show_box};
-use layer::{EguiMapFrame, Layer, TimelineLayer};
+use layer::{EguiLayer, EguiMapFrame, TimelineLayer};
 use log::{debug, warn};
 
 use super::{
@@ -38,7 +38,7 @@ pub use layer::ParameterUpdate;
 
 pub struct Map {
   transform: Transform,
-  layers: Rc<Mutex<Vec<Box<dyn Layer>>>>,
+  layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>>,
   timeline: Rc<RefCell<TimelineLayer>>,
   recv: Rc<Receiver<MapEvent>>,
   layer_sender: Sender<MapEvent>,
@@ -162,7 +162,7 @@ impl Map {
       acc
     });
 
-    let mut layer_vec: Vec<Box<dyn Layer>> = Vec::new();
+    let mut layer_vec: Vec<Box<dyn EguiLayer>> = Vec::new();
     if let Some(tl) = tile_layer {
       layer_vec.push(Box::new(tl));
     }
@@ -171,7 +171,7 @@ impl Map {
     if let Some(screenshot_layer) = screenshot_layer {
       layer_vec.push(Box::new(screenshot_layer));
     }
-    let layers: Rc<Mutex<Vec<Box<dyn Layer>>>> = Rc::new(Mutex::new(layer_vec));
+    let layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>> = Rc::new(Mutex::new(layer_vec));
 
     let map_data_holder = Rc::new(MapLayerHolderImpl::new(layers.clone(), timeline.clone()));
 
@@ -765,7 +765,11 @@ impl Map {
     );
   }
 
-  fn draw_egui_layer_overlays(layers: &mut [Box<dyn Layer>], ui: &mut Ui, transform: &Transform) {
+  fn draw_egui_layer_overlays(
+    layers: &mut [Box<dyn EguiLayer>],
+    ui: &mut Ui,
+    transform: &Transform,
+  ) {
     for layer in layers {
       profile_scope!("Layer::draw_egui_overlay", layer.name());
       layer.draw_egui_overlay(ui, transform);
@@ -773,7 +777,7 @@ impl Map {
   }
 
   fn draw_egui_layers(
-    layers: &mut [Box<dyn Layer>],
+    layers: &mut [Box<dyn EguiLayer>],
     timeline: &RefCell<TimelineLayer>,
     ui: &mut Ui,
     transform: &Transform,
@@ -1206,7 +1210,7 @@ impl Map {
 }
 
 pub trait MapLayerReader {
-  fn get_layers(&mut self) -> Box<dyn Iterator<Item = &mut Box<dyn Layer>> + '_>;
+  fn get_layers(&mut self) -> Box<dyn Iterator<Item = &mut Box<dyn EguiLayer>> + '_>;
 }
 
 pub trait MapLayerHolder {
@@ -1223,11 +1227,11 @@ pub trait MapLayerHolder {
 }
 
 struct MapLayerHolderImpl {
-  layers: Rc<Mutex<Vec<Box<dyn Layer>>>>,
+  layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>>,
   timeline: Rc<RefCell<TimelineLayer>>,
 }
 impl MapLayerHolderImpl {
-  fn new(layers: Rc<Mutex<Vec<Box<dyn Layer>>>>, timeline: Rc<RefCell<TimelineLayer>>) -> Self {
+  fn new(layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>>, timeline: Rc<RefCell<TimelineLayer>>) -> Self {
     Self { layers, timeline }
   }
 }
@@ -1256,10 +1260,10 @@ impl MapLayerHolder for MapLayerHolderImpl {
   }
 }
 
-struct MapLayerReaderImpl<'a>(MutexGuard<'a, Vec<Box<dyn Layer>>>);
+struct MapLayerReaderImpl<'a>(MutexGuard<'a, Vec<Box<dyn EguiLayer>>>);
 
 impl MapLayerReader for MapLayerReaderImpl<'_> {
-  fn get_layers(&mut self) -> Box<dyn Iterator<Item = &mut Box<dyn Layer>> + '_> {
+  fn get_layers(&mut self) -> Box<dyn Iterator<Item = &mut Box<dyn EguiLayer>> + '_> {
     Box::new(self.0.iter_mut())
   }
 }
