--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -77,12 +77,6 @@
     self
       .map
       .set_external_viewport_input(external_viewport_input);
-  }
-
-  pub fn set_native_geometry_rendering(&mut self, native_geometry_rendering: bool) {
-    self
-      .map
-      .set_native_geometry_rendering(native_geometry_rendering);
   }
 
   pub fn set_map_transform(&mut self, transform: crate::map::coordinates::Transform) {
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -719,10 +719,6 @@
 
   pub fn set_external_viewport_input(&mut self, external_viewport_input: bool) {
     self.external_viewport_input = external_viewport_input;
-  }
-
-  pub fn set_native_geometry_rendering(&mut self, native_geometry_rendering: bool) {
-    self.native_geometry_rendering = native_geometry_rendering;
   }
 
   pub fn set_transform(&mut self, transform: Transform) {
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -36,6 +36,7 @@
 32. `032-native-coordinate-overlay-labels.patch`
 33. `033-native-osm-attribution-overlay.patch`
 34. `034-native-rendering-map-constructor.patch`
+35. `035-remove-native-rendering-setter.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
