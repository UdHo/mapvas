--- a/Cargo.toml
+++ b/Cargo.toml
@@ -55,6 +55,7 @@
 json5 = "1.3.0"
 egui_kittest = { version = "0.33.3", features = ["snapshot", "wgpu"] }
 rstar = "0.13.0"
+earcut = "0.4.5"
 
 [features]
 default = []
--- a/Cargo.lock
+++ b/Cargo.lock
@@ -2944,6 +2944,15 @@
 version = "0.1.2"
 source = "registry+https://github.com/rust-lang/crates.io-index"
 checksum = "d8b14ccef22fc6f5a8f4d7d768562a182c04ce9a3b3157b91390b52ddfdf1a76"
+
+[[package]]
+name = "earcut"
+version = "0.4.10"
+source = "registry+https://github.com/rust-lang/crates.io-index"
+checksum = "7d71866fffb1c6d539cc36ef323109416cdc5686e17df0252d98c5e83641efc8"
+dependencies = [
+ "num-traits",
+]
 
 [[package]]
 name = "ecolor"
@@ -4874,6 +4883,7 @@
  "chrono",
  "clap",
  "dirs",
+ "earcut",
  "eframe",
  "egui",
  "egui_extras",
--- a/src/bin/mapvas/bevy_geometry.rs
+++ b/src/bin/mapvas/bevy_geometry.rs
@@ -307,6 +307,17 @@
 fn polygon_fill_mesh(coords: &[PixelCoordinate]) -> Option<Mesh> {
   let vertices = polygon_vertices(coords);
   if vertices.len() < 3 {
+    return None;
+  }
+
+  let mut indices = Vec::with_capacity((vertices.len() - 2) * 3);
+  let mut earcut = earcut::Earcut::new();
+  earcut.earcut(
+    vertices.iter().map(|coord| [coord.x, coord.y]),
+    &[],
+    &mut indices,
+  );
+  if indices.is_empty() {
     return None;
   }
 
@@ -316,11 +327,6 @@
     .collect::<Vec<_>>();
   let normals = vec![[0.0, 0.0, 1.0]; vertices.len()];
   let uvs = vec![[0.0, 0.0]; vertices.len()];
-  let mut indices = Vec::with_capacity((vertices.len() - 2) * 3);
-  for index in 1..vertices.len() - 1 {
-    let index = u32::try_from(index).ok()?;
-    indices.extend_from_slice(&[0, index, index + 1]);
-  }
 
   Some(
     Mesh::new(
