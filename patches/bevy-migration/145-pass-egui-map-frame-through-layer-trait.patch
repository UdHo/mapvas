diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 0578403..28c5969 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -146,6 +146,7 @@ Apply these patches in order from the repository root:
 142. `142-use-pixel-rect-for-highlight-rasterizer.patch`
 143. `143-use-pixel-rect-for-egui-tile-paint-helpers.patch`
 144. `144-use-pixel-rect-for-shape-tile-paint.patch`
+145. `145-pass-egui-map-frame-through-layer-trait.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/commands.rs b/src/map/mapvas_egui/layer/commands.rs
index d1c19fa..598ce9b 100644
--- a/src/map/mapvas_egui/layer/commands.rs
+++ b/src/map/mapvas_egui/layer/commands.rs
@@ -9,7 +9,7 @@ use serde::{Deserialize, Serialize};
 
 use crate::map::coordinates::{BoundingBox, PixelCoordinate, PixelPosition, Transform};
 
-use super::{GeometrySnapshot, Layer, LayerProperties, drawable::Drawable};
+use super::{EguiMapFrame, GeometrySnapshot, Layer, LayerProperties, drawable::Drawable};
 
 mod external_cmd;
 mod ruler;
@@ -231,7 +231,7 @@ impl Layer for CommandLayer {
     &mut self,
     ui: &mut egui::Ui,
     transform: &crate::map::coordinates::Transform,
-    _rect: egui::Rect,
+    _frame: EguiMapFrame,
   ) {
     if self.visible() {
       for command in self.commands.iter().filter(|command| command.is_visible()) {
diff --git a/src/map/mapvas_egui/layer/screenshot.rs b/src/map/mapvas_egui/layer/screenshot.rs
index 8dcde39..d8ad76c 100644
--- a/src/map/mapvas_egui/layer/screenshot.rs
+++ b/src/map/mapvas_egui/layer/screenshot.rs
@@ -6,7 +6,7 @@ use egui::{
 
 use crate::map::{map_event::MapEvent, mapvas_egui::helpers::current_time_screenshot_name};
 
-use super::{Layer, LayerProperties};
+use super::{EguiMapFrame, Layer, LayerProperties};
 
 /// A layer that takes screenshots of the map, saves the image, and displays it on the map.
 pub struct ScreenshotLayer {
@@ -124,13 +124,14 @@ impl Layer for ScreenshotLayer {
     &mut self,
     ui: &mut egui::Ui,
     _transform: &crate::map::coordinates::Transform,
-    rect: egui::Rect,
+    frame: EguiMapFrame,
   ) {
     if !self.visible() {
       return;
     }
 
     if let Some(texture) = &self.last_screenshot {
+      let rect = frame.rect;
       let screenshot_rect = rect
         .with_min_x(rect.max.x - (rect.max.x - rect.min.x) * 0.2)
         .with_min_y(rect.max.y - (rect.max.y - rect.min.y) * 0.2);
diff --git a/src/map/mapvas_egui/layer/shape_layer.rs b/src/map/mapvas_egui/layer/shape_layer.rs
index b8caca8..7c4d066 100644
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -1,6 +1,6 @@
-use super::super::helpers::{pixel_to_pos, pos_to_pixel, rect_to_pixel};
+use super::super::helpers::{pixel_to_pos, pos_to_pixel};
 use super::{
-  GeometrySnapshot, Layer, LayerProperties, Searchable, SubLayerInfo,
+  EguiMapFrame, GeometrySnapshot, Layer, LayerProperties, Searchable, SubLayerInfo,
   geometry_highlighting::GeometryHighlighter, geometry_rasterizer,
 };
 use rstar::{AABB, RTree, RTreeObject};
@@ -20,7 +20,7 @@ use crate::{
   task_tracker::{TaskCategory, TaskGuard},
 };
 use chrono::{DateTime, Duration, Utc};
-use egui::{Color32, ColorImage, Rect, Ui};
+use egui::{Color32, ColorImage, Ui};
 use regex::Regex;
 use std::{
   collections::{HashMap, HashSet},
@@ -774,7 +774,7 @@ impl Layer for ShapeLayer {
     self.update_hover_highlighting(pos, transform);
   }
 
-  fn draw_egui_overlay(&mut self, ui: &mut Ui, transform: &Transform, _rect: Rect) {
+  fn draw_egui_overlay(&mut self, ui: &mut Ui, transform: &Transform, _frame: EguiMapFrame) {
     profile_scope!("ShapeLayer::draw_egui_overlay");
 
     self.current_transform = *transform;
@@ -786,8 +786,10 @@ impl Layer for ShapeLayer {
   }
 
   #[allow(clippy::too_many_lines)]
-  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, rect: Rect) {
+  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
     profile_scope!("ShapeLayer::draw_egui");
+    let rect = frame.rect;
+    let pixel_rect = frame.pixel_rect;
 
     if !self.prepare_egui_draw_frame(ui, transform) {
       return;
@@ -808,7 +810,6 @@ impl Layer for ShapeLayer {
     });
     let use_sync_render = self.headless || (!has_heatmap && total_geometries <= 10_000);
     let painter = ui.painter_at(rect);
-    let pixel_rect = rect_to_pixel(rect);
     for tile in Self::compute_visible_geo_tiles(transform, pixel_rect) {
       // Quick reject: check if the R-tree has any geometry overlapping this tile.
       let (nw, se) = tile.position();
diff --git a/src/map/mapvas_egui/layer/tile_layer.rs b/src/map/mapvas_egui/layer/tile_layer.rs
index 9e85a73..65bc770 100644
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -22,8 +22,8 @@ use crate::{
   task_tracker::{TaskCategory, TaskGuard},
 };
 
-use super::{Layer, LayerProperties};
-use crate::map::mapvas_egui::helpers::{pixel_to_pos, rect_to_pixel};
+use super::{EguiMapFrame, Layer, LayerProperties};
+use crate::map::mapvas_egui::helpers::pixel_to_pos;
 
 /// Compute the pixel x-coordinate of the viewport's left edge in internal canvas coordinates.
 fn viewport_left_x(rect: PixelRect, transform: &Transform) -> f32 {
@@ -751,8 +751,10 @@ impl Layer for TileLayer {
     clippy::cast_sign_loss,
     clippy::too_many_lines
   )]
-  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, rect: Rect) {
+  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
     profile_scope!("TileLayer::draw_egui");
+    let rect = frame.rect;
+    let pixel_rect = frame.pixel_rect;
     self.collect_new_tile_data(ui);
     if self.tile_loader_index != self.tile_loader_old_index {
       log::info!(
@@ -802,7 +804,6 @@ impl Layer for TileLayer {
       log::debug!("Zoom: ideal={calculated_zoom}, max={max_zoom}, request_zoom={request_zoom}");
     }
 
-    let pixel_rect = rect_to_pixel(rect);
     let inv = transform.invert();
     let vp_min = inv.apply(pixel_rect.min);
     let vp_max = inv.apply(pixel_rect.max);
@@ -987,12 +988,12 @@ impl Layer for TileLayer {
     self.preload_enabled = false;
   }
 
-  fn draw_egui_attribution(&self, ui: &mut Ui, rect: Rect) {
+  fn draw_egui_attribution(&self, ui: &mut Ui, frame: EguiMapFrame) {
     if self.coordinate_display_mode != CoordinateDisplayMode::GridOnly
       && !self.loaded_tiles.is_empty()
       && self.tile_loader().requires_osm_attribution()
     {
-      draw_osm_attribution(ui, rect);
+      draw_osm_attribution(ui, frame.rect);
     }
   }
 }
diff --git a/src/map/mapvas_egui/layer/timeline_layer.rs b/src/map/mapvas_egui/layer/timeline_layer.rs
index 9764645..a6c7f6c 100644
--- a/src/map/mapvas_egui/layer/timeline_layer.rs
+++ b/src/map/mapvas_egui/layer/timeline_layer.rs
@@ -1,4 +1,4 @@
-use super::{Layer, LayerProperties};
+use super::{EguiMapFrame, Layer, LayerProperties};
 use crate::map::coordinates::{BoundingBox, Transform};
 use crate::map::mapvas_egui::timeline_widget::TimelineWidget;
 use chrono::{DateTime, Utc};
@@ -207,10 +207,11 @@ impl TimelineLayer {
 }
 
 impl Layer for TimelineLayer {
-  fn draw_egui(&mut self, ui: &mut Ui, _transform: &Transform, rect: Rect) {
+  fn draw_egui(&mut self, ui: &mut Ui, _transform: &Transform, frame: EguiMapFrame) {
     if !self.properties.visible {
       return;
     }
+    let rect = frame.rect;
 
     // Update animation
     let current_time = ui.input(|i| i.time);
diff --git a/src/map/mapvas_egui/layer.rs b/src/map/mapvas_egui/layer.rs
index 6b009e6..e16ed0c 100644
--- a/src/map/mapvas_egui/layer.rs
+++ b/src/map/mapvas_egui/layer.rs
@@ -1,4 +1,4 @@
-use crate::map::coordinates::{BoundingBox, PixelPosition, Transform};
+use crate::map::coordinates::{BoundingBox, PixelPosition, PixelRect, Transform};
 use egui::{Rect, Ui};
 
 /// Allows to display results of commands that return coordinates.
@@ -34,11 +34,36 @@ pub struct SubLayerInfo {
   pub shape_count: usize,
 }
 
+#[derive(Debug, Clone, Copy)]
+pub struct EguiMapFrame {
+  pub rect: Rect,
+  pub pixel_rect: PixelRect,
+}
+
+impl EguiMapFrame {
+  #[must_use]
+  pub fn from_rect(rect: Rect) -> Self {
+    Self {
+      rect,
+      pixel_rect: PixelRect::from_min_max(
+        PixelPosition {
+          x: rect.min.x,
+          y: rect.min.y,
+        },
+        PixelPosition {
+          x: rect.max.x,
+          y: rect.max.y,
+        },
+      ),
+    }
+  }
+}
+
 /// A layer represents everything that can be summarized as a logical unit on the map.
 /// E.g. a layer to draw the map tiles and one to draw the shapes.
 pub trait Layer {
-  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, rect: Rect);
-  fn draw_egui_overlay(&mut self, _ui: &mut Ui, _transform: &Transform, _rect: Rect) {}
+  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame);
+  fn draw_egui_overlay(&mut self, _ui: &mut Ui, _transform: &Transform, _frame: EguiMapFrame) {}
   fn handle_hover(&mut self, _pos: Option<PixelPosition>, _transform: &Transform) {}
   fn clear(&mut self) {}
   fn name(&self) -> &str;
@@ -120,7 +145,7 @@ pub trait Layer {
   fn set_headless(&mut self) {}
 
   /// Draw egui attribution or licensing overlays after all map content is painted.
-  fn draw_egui_attribution(&self, _ui: &mut Ui, _rect: Rect) {}
+  fn draw_egui_attribution(&self, _ui: &mut Ui, _frame: EguiMapFrame) {}
 
   /// Return info about named sub-layers (e.g. each id sent via `mapcat`).
   fn sub_layers(&self) -> Vec<SubLayerInfo> {
diff --git a/src/map/mapvas_egui.rs b/src/map/mapvas_egui.rs
index 2ae3190..fae6be4 100644
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -21,7 +21,7 @@ use crate::{
 use arboard::Clipboard;
 use egui::{InputState, PointerButton, Rect, Response, Sense, Ui};
 use helpers::{pixel_to_pos, point_to_coordinate, pos_to_pixel, rect_to_pixel, show_box};
-use layer::{Layer, TimelineLayer};
+use layer::{EguiMapFrame, Layer, TimelineLayer};
 use log::{debug, warn};
 
 use super::{
@@ -740,7 +740,12 @@ impl Map {
     for layer in layer_guard.iter_mut() {
       layer.process_pending_events();
     }
-    Self::draw_egui_layer_overlays(layer_guard.as_mut_slice(), ui, &self.transform, rect);
+    Self::draw_egui_layer_overlays(
+      layer_guard.as_mut_slice(),
+      ui,
+      &self.transform,
+      EguiMapFrame::from_rect(rect),
+    );
   }
 
   fn draw_egui_layer_frame(&mut self, ui: &mut Ui, rect: Rect) {
@@ -760,7 +765,7 @@ impl Map {
       &self.timeline,
       ui,
       &self.transform,
-      rect,
+      EguiMapFrame::from_rect(rect),
     );
   }
 
@@ -768,11 +773,11 @@ impl Map {
     layers: &mut [Box<dyn Layer>],
     ui: &mut Ui,
     transform: &Transform,
-    rect: Rect,
+    frame: EguiMapFrame,
   ) {
     for layer in layers {
       profile_scope!("Layer::draw_egui_overlay", layer.name());
-      layer.draw_egui_overlay(ui, transform, rect);
+      layer.draw_egui_overlay(ui, transform, frame);
     }
   }
 
@@ -781,20 +786,20 @@ impl Map {
     timeline: &RefCell<TimelineLayer>,
     ui: &mut Ui,
     transform: &Transform,
-    rect: Rect,
+    frame: EguiMapFrame,
   ) {
     for layer in layers.iter_mut() {
       layer.process_pending_events();
       profile_scope!("Layer::draw_egui", layer.name());
-      layer.draw_egui(ui, transform, rect);
+      layer.draw_egui(ui, transform, frame);
     }
 
     profile_scope!("Layer::draw_egui", "Timeline");
-    timeline.borrow_mut().draw_egui(ui, transform, rect);
+    timeline.borrow_mut().draw_egui(ui, transform, frame);
     for layer in layers.iter() {
-      layer.draw_egui_attribution(ui, rect);
+      layer.draw_egui_attribution(ui, frame);
     }
-    Self::draw_egui_layer_overlays(layers, ui, transform, rect);
+    Self::draw_egui_layer_overlays(layers, ui, transform, frame);
   }
 
   fn allocate_map_response(ui: &mut Ui, sense: Sense) -> (Rect, Response) {
