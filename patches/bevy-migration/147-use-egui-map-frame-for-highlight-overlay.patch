diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 0cc1924..52c8d1d 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -148,6 +148,7 @@ Apply these patches in order from the repository root:
 144. `144-use-pixel-rect-for-shape-tile-paint.patch`
 145. `145-pass-egui-map-frame-through-layer-trait.patch`
 146. `146-thread-egui-map-frame-through-map-widget.patch`
+147. `147-use-egui-map-frame-for-highlight-overlay.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/helpers.rs b/src/map/mapvas_egui/helpers.rs
index ccc26f8..9e3b78a 100644
--- a/src/map/mapvas_egui/helpers.rs
+++ b/src/map/mapvas_egui/helpers.rs
@@ -1,7 +1,5 @@
 use std::path::PathBuf;
 
-use egui::Rect;
-
 use crate::map::{
   color::Color,
   coordinates::{BoundingBox, PixelCoordinate, PixelPosition, PixelRect, Transform},
@@ -25,10 +23,6 @@ pub(crate) fn pixel_to_pos(pos: PixelPosition) -> egui::Pos2 {
   egui::pos2(pos.x, pos.y)
 }
 
-pub(crate) fn rect_to_pixel(rect: Rect) -> PixelRect {
-  PixelRect::from_min_max(pos_to_pixel(rect.min), pos_to_pixel(rect.max))
-}
-
 /// Converts a point, e.g. from a click, to a coordinate.
 pub(crate) fn point_to_coordinate(point: PixelPosition, transform: &Transform) -> PixelCoordinate {
   let inv = transform.invert();
diff --git a/src/map/mapvas_egui/layer/shape_layer/sidebar.rs b/src/map/mapvas_egui/layer/shape_layer/sidebar.rs
index 01cf48c..104eada 100644
--- a/src/map/mapvas_egui/layer/shape_layer/sidebar.rs
+++ b/src/map/mapvas_egui/layer/shape_layer/sidebar.rs
@@ -3,7 +3,10 @@ use crate::map::{
   color::Color as MapColor,
   coordinates::{Coordinate, PixelCoordinate, Transform, WGS84Coordinate},
   geometry_collection::{Geometry, Metadata, Style},
-  mapvas_egui::helpers::{color_to_color32, color32_to_color, pixel_to_pos, rect_to_pixel},
+  mapvas_egui::{
+    helpers::{color_to_color32, color32_to_color, pixel_to_pos},
+    layer::EguiMapFrame,
+  },
 };
 use egui::{Color32, Rect};
 
@@ -1266,8 +1269,9 @@ impl ShapeLayer {
     &mut self,
     ui: &mut egui::Ui,
     transform: &Transform,
-    rect: Rect,
+    frame: EguiMapFrame,
   ) {
+    let rect = frame.rect;
     let Some((layer_id, shape_idx, nested_path)) =
       self.geometry_highlighter.get_highlighted_geometry()
     else {
@@ -1301,7 +1305,7 @@ impl ShapeLayer {
       return;
     };
 
-    let pixel_rect = rect_to_pixel(rect);
+    let pixel_rect = frame.pixel_rect;
     let key = HighlightCacheKey {
       geometry_path: (layer_id, shape_idx, nested_path),
       viewport: [
diff --git a/src/map/mapvas_egui/layer/shape_layer.rs b/src/map/mapvas_egui/layer/shape_layer.rs
index 7c4d066..c3262ee 100644
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -934,7 +934,7 @@ impl Layer for ShapeLayer {
     // Draw highlight overlay for the currently-hovered geometry.
     // Polygon fills go through tiny-skia (cached as a texture) to avoid egui's
     // fan-triangulation; points, lines, and polygon strokes go through egui.
-    self.draw_highlight_overlay(ui, transform, rect);
+    self.draw_highlight_overlay(ui, transform, frame);
   }
 
   fn bounding_box(&self) -> Option<BoundingBox> {
