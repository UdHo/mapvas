diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index a10f744..b738302 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -175,6 +175,7 @@ Apply these patches in order from the repository root:
 171. `171-remove-egui-layer-neutral-reexports.patch`
 172. `172-add-neutral-layer-iteration-helpers.patch`
 173. `173-route-neutral-map-methods-through-layer-helpers.patch`
+174. `174-encapsulate-map-layer-store.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui.rs b/src/map/mapvas_egui.rs
index 76b56e0..813db93 100644
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -39,7 +39,7 @@ pub use layer::ParameterUpdate;
 
 pub struct Map {
   transform: Transform,
-  layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>>,
+  layers: MapLayerStore,
   timeline: Rc<RefCell<TimelineLayer>>,
   recv: Rc<Receiver<MapEvent>>,
   layer_sender: Sender<MapEvent>,
@@ -61,6 +61,77 @@ enum ScreenshotTarget {
   Native(Vec<PathBuf>),
 }
 
+#[derive(Clone)]
+struct MapLayerStore {
+  layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>>,
+}
+
+impl MapLayerStore {
+  fn new(layers: Vec<Box<dyn EguiLayer>>) -> Self {
+    Self {
+      layers: Rc::new(Mutex::new(layers)),
+    }
+  }
+
+  fn for_each_neutral_mut(&self, mut f: impl FnMut(&mut dyn NeutralLayer)) {
+    if let Ok(mut layers) = self.layers.lock() {
+      for layer in layers.iter_mut() {
+        f(layer.as_mut());
+      }
+    }
+  }
+
+  fn fold_neutral<R>(&self, initial: R, mut f: impl FnMut(R, &dyn NeutralLayer) -> R) -> R {
+    let Ok(layers) = self.layers.lock() else {
+      return initial;
+    };
+    layers
+      .iter()
+      .fold(initial, |acc, layer| f(acc, layer.as_ref()))
+  }
+
+  fn find_neutral<R>(&self, mut f: impl FnMut(&dyn NeutralLayer) -> Option<R>) -> Option<R> {
+    let Ok(layers) = self.layers.lock() else {
+      return None;
+    };
+    layers.iter().find_map(|layer| f(layer.as_ref()))
+  }
+
+  fn find_neutral_mut<R>(
+    &self,
+    mut f: impl FnMut(&mut dyn NeutralLayer) -> Option<R>,
+  ) -> Option<R> {
+    let Ok(mut layers) = self.layers.lock() else {
+      return None;
+    };
+    layers.iter_mut().find_map(|layer| f(layer.as_mut()))
+  }
+
+  fn any_neutral(&self, mut predicate: impl FnMut(&dyn NeutralLayer) -> bool) -> bool {
+    self
+      .find_neutral(|layer| predicate(layer).then_some(()))
+      .is_some()
+  }
+
+  fn try_with_egui_layers_mut(&self, context: &str, f: impl FnOnce(&mut [Box<dyn EguiLayer>])) {
+    let Ok(mut layers) = self.layers.try_lock().inspect_err(|e| {
+      log::error!("Failed to lock layers for {context}: {e:?}");
+    }) else {
+      return;
+    };
+    f(layers.as_mut_slice());
+  }
+
+  fn egui_reader(&self) -> EguiLayerReaderImpl<'_> {
+    EguiLayerReaderImpl(
+      self
+        .layers
+        .lock()
+        .unwrap_or_else(std::sync::PoisonError::into_inner),
+    )
+  }
+}
+
 #[derive(Clone, Copy)]
 enum MapConstructionMode {
   Egui { include_tiles: bool },
@@ -172,7 +243,7 @@ impl Map {
     if let Some(screenshot_layer) = screenshot_layer {
       layer_vec.push(Box::new(screenshot_layer));
     }
-    let layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>> = Rc::new(Mutex::new(layer_vec));
+    let layers = MapLayerStore::new(layer_vec);
 
     let map_data_holder = Rc::new(MapLayerHolderImpl::new(layers.clone(), timeline.clone()));
 
@@ -214,7 +285,7 @@ impl Map {
     log::debug!("Map: Double-click detected at {pos:?}");
 
     let transform = self.transform;
-    self.for_each_layer_mut(|layer| {
+    self.layers.for_each_neutral_mut(|layer| {
       if let Some(distance) = layer.closest_geometry_with_selection(pos, &transform) {
         log::debug!(
           "Map: Layer '{}' can handle at distance: {:.2}",
@@ -301,7 +372,7 @@ impl Map {
 
   pub fn handle_native_hover(&mut self, pos: Option<PixelPosition>) {
     let transform = self.transform;
-    self.for_each_layer_mut(|layer| {
+    self.layers.for_each_neutral_mut(|layer| {
       layer.handle_hover(pos, &transform);
     });
   }
@@ -359,9 +430,11 @@ impl Map {
   }
 
   fn show_bounding_box(&mut self, rect: PixelRect) {
-    let mut bb = self.fold_layers(BoundingBox::get_invalid(), |acc, layer| {
-      layer.bounding_box().map_or(acc, |bb| acc.extend(&bb))
-    });
+    let mut bb = self
+      .layers
+      .fold_neutral(BoundingBox::get_invalid(), |acc, layer| {
+        layer.bounding_box().map_or(acc, |bb| acc.extend(&bb))
+      });
     if bb.is_valid() {
       if !bb.is_box() {
         bb.frame(0.02);
@@ -378,11 +451,13 @@ impl Map {
   }
 
   fn show_layer_bounding_box(&mut self, id: &str, rect: PixelRect) {
-    let mut bb = self.fold_layers(BoundingBox::get_invalid(), |acc, layer| {
-      layer
-        .sub_layer_bounding_box(id)
-        .map_or(acc, |bb| acc.extend(&bb))
-    });
+    let mut bb = self
+      .layers
+      .fold_neutral(BoundingBox::get_invalid(), |acc, layer| {
+        layer
+          .sub_layer_bounding_box(id)
+          .map_or(acc, |bb| acc.extend(&bb))
+      });
     if bb.is_valid() {
       if !bb.is_box() {
         bb.frame(0.02);
@@ -392,7 +467,9 @@ impl Map {
   }
 
   fn show_shape_bounding_box(&mut self, layer_id: &str, shape_idx: usize, rect: PixelRect) {
-    let Some(mut bb) = self.find_map_layer(|layer| layer.shape_bounding_box(layer_id, shape_idx))
+    let Some(mut bb) = self
+      .layers
+      .find_neutral(|layer| layer.shape_bounding_box(layer_id, shape_idx))
     else {
       return;
     };
@@ -555,7 +632,7 @@ impl Map {
 
   fn process_pending_layer_data(&mut self) {
     // Force all layers to process any pending data immediately
-    self.for_each_layer_mut(|layer| {
+    self.layers.for_each_neutral_mut(|layer| {
       // For shape layers, this will process any pending MapEvent::Layer events
       // For other layers, this is typically a no-op
       layer.process_pending_events();
@@ -564,10 +641,12 @@ impl Map {
 
   pub fn clear(&mut self) {
     // First, discard any pending layer events to prevent them from being processed later
-    self.for_each_layer_mut(|layer| layer.discard_pending_events());
+    self
+      .layers
+      .for_each_neutral_mut(|layer| layer.discard_pending_events());
 
     // Then clear the actual layer data
-    self.for_each_layer_mut(|layer| layer.clear());
+    self.layers.for_each_neutral_mut(|layer| layer.clear());
   }
 
   pub fn handle_dropped_file(&self, file_path: PathBuf) {
@@ -686,16 +765,15 @@ impl Map {
       return;
     }
 
-    let Ok(mut layer_guard) = self.layers.try_lock().inspect_err(|e| {
-      log::error!("Failed to lock layers: {e:?}");
-    }) else {
-      return;
-    };
-
-    for layer in layer_guard.iter_mut() {
-      layer.process_pending_events();
-    }
-    Self::draw_egui_layer_overlays(layer_guard.as_mut_slice(), ui, &self.transform);
+    let transform = self.transform;
+    self
+      .layers
+      .try_with_egui_layers_mut("native layer frame", |layers| {
+        for layer in layers.iter_mut() {
+          layer.process_pending_events();
+        }
+        Self::draw_egui_layer_overlays(layers, ui, &transform);
+      });
   }
 
   fn draw_egui_layer_frame(&mut self, ui: &mut Ui, frame: EguiMapFrame) {
@@ -705,19 +783,13 @@ impl Map {
       return;
     }
 
-    let Ok(mut layer_guard) = self.layers.try_lock().inspect_err(|e| {
-      log::error!("Failed to lock layers: {e:?}");
-    }) else {
-      return;
-    };
-
-    Self::draw_egui_layers(
-      layer_guard.as_mut_slice(),
-      &self.timeline,
-      ui,
-      &self.transform,
-      frame,
-    );
+    let transform = self.transform;
+    let timeline = self.timeline.clone();
+    self
+      .layers
+      .try_with_egui_layers_mut("egui layer frame", |layers| {
+        Self::draw_egui_layers(layers, &timeline, ui, &transform, frame);
+      });
   }
 
   fn draw_egui_layer_overlays(
@@ -814,76 +886,42 @@ impl Map {
 }
 
 impl Map {
-  fn for_each_layer_mut(&mut self, mut f: impl FnMut(&mut dyn NeutralLayer)) {
-    if let Ok(mut layers) = self.layers.lock() {
-      for layer in layers.iter_mut() {
-        f(layer.as_mut());
-      }
-    }
-  }
-
-  fn fold_layers<R>(&self, initial: R, mut f: impl FnMut(R, &dyn NeutralLayer) -> R) -> R {
-    let Ok(layers) = self.layers.lock() else {
-      return initial;
-    };
-    layers
-      .iter()
-      .fold(initial, |acc, layer| f(acc, layer.as_ref()))
-  }
-
-  fn find_map_layer<R>(&self, mut f: impl FnMut(&dyn NeutralLayer) -> Option<R>) -> Option<R> {
-    let Ok(layers) = self.layers.lock() else {
-      return None;
-    };
-    layers.iter().find_map(|layer| f(layer.as_ref()))
-  }
-
-  fn find_map_layer_mut<R>(
-    &mut self,
-    mut f: impl FnMut(&mut dyn NeutralLayer) -> Option<R>,
-  ) -> Option<R> {
-    let Ok(mut layers) = self.layers.lock() else {
-      return None;
-    };
-    layers.iter_mut().find_map(|layer| f(layer.as_mut()))
-  }
-
-  fn any_layer(&self, mut predicate: impl FnMut(&dyn NeutralLayer) -> bool) -> bool {
-    self
-      .find_map_layer(|layer| predicate(layer).then_some(()))
-      .is_some()
-  }
-
   pub fn set_headless(&mut self) {
-    self.for_each_layer_mut(|layer| layer.set_headless());
+    self
+      .layers
+      .for_each_neutral_mut(|layer| layer.set_headless());
   }
 
   #[must_use]
   pub fn has_pending_work(&self) -> bool {
-    self.any_layer(|layer| layer.has_pending_work())
+    self.layers.any_neutral(|layer| layer.has_pending_work())
   }
 
   /// Check if a double-click just occurred on any layer
   #[must_use]
   pub fn has_double_click_action(&self) -> bool {
-    self.any_layer(|layer| layer.has_double_click_action())
+    self
+      .layers
+      .any_neutral(|layer| layer.has_double_click_action())
   }
 
   /// Refresh the shared `MapState` snapshot that the HTTP `/state` endpoint serves.
   pub fn update_state_snapshot(&self) {
-    let sub_layers = self.fold_layers(Vec::new(), |mut sub_layers, layer| {
-      sub_layers.extend(
-        layer
-          .sub_layers()
-          .into_iter()
-          .map(|s| crate::remote::LayerInfo {
-            id: s.id,
-            visible: s.visible,
-            shape_count: s.shape_count,
-          }),
-      );
-      sub_layers
-    });
+    let sub_layers = self
+      .layers
+      .fold_neutral(Vec::new(), |mut sub_layers, layer| {
+        sub_layers.extend(
+          layer
+            .sub_layers()
+            .into_iter()
+            .map(|s| crate::remote::LayerInfo {
+              id: s.id,
+              visible: s.visible,
+              shape_count: s.shape_count,
+            }),
+        );
+        sub_layers
+      });
     let Ok(mut state) = self.map_state.try_write() else {
       return;
     };
@@ -908,35 +946,37 @@ impl Map {
     &self,
     known_geometry_version: Option<u64>,
   ) -> GeometrySnapshot {
-    let geometry_version = self.fold_layers(0_u64, |version, layer| {
+    let geometry_version = self.layers.fold_neutral(0_u64, |version, layer| {
       version
         .wrapping_mul(31)
         .wrapping_add(layer.geometry_base_snapshot_version())
     });
     let include_geometries = known_geometry_version != Some(geometry_version);
 
-    self.fold_layers(GeometrySnapshot::default(), |mut acc, layer| {
-      let mut snapshot = if include_geometries {
-        layer.geometry_snapshot()
-      } else {
-        layer.geometry_snapshot_without_geometries()
-      };
-      acc.version = acc.version.wrapping_mul(31).wrapping_add(snapshot.version);
-      acc.geometry_version = acc
-        .geometry_version
-        .wrapping_mul(31)
-        .wrapping_add(snapshot.geometry_version);
-      acc.geometries.append(&mut snapshot.geometries);
-      acc
-        .highlighted_geometries
-        .append(&mut snapshot.highlighted_geometries);
-      acc
-    })
+    self
+      .layers
+      .fold_neutral(GeometrySnapshot::default(), |mut acc, layer| {
+        let mut snapshot = if include_geometries {
+          layer.geometry_snapshot()
+        } else {
+          layer.geometry_snapshot_without_geometries()
+        };
+        acc.version = acc.version.wrapping_mul(31).wrapping_add(snapshot.version);
+        acc.geometry_version = acc
+          .geometry_version
+          .wrapping_mul(31)
+          .wrapping_add(snapshot.geometry_version);
+        acc.geometries.append(&mut snapshot.geometries);
+        acc
+          .highlighted_geometries
+          .append(&mut snapshot.highlighted_geometries);
+        acc
+      })
   }
 
   #[must_use]
   pub fn geometry_snapshot_version(&self) -> u64 {
-    self.fold_layers(0, |version, layer| {
+    self.layers.fold_neutral(0, |version, layer| {
       version
         .wrapping_mul(31)
         .wrapping_add(layer.geometry_snapshot_version())
@@ -948,7 +988,9 @@ impl Map {
     self.config = new_config.clone();
 
     // Update all layers that need config updates
-    self.for_each_layer_mut(|layer| layer.update_config(new_config));
+    self
+      .layers
+      .for_each_neutral_mut(|layer| layer.update_config(new_config));
   }
 
   /// Set temporal filtering for all layers
@@ -957,7 +999,9 @@ impl Map {
     current_time: Option<chrono::DateTime<chrono::Utc>>,
     time_window: Option<chrono::Duration>,
   ) {
-    self.for_each_layer_mut(|layer| layer.set_temporal_filter(current_time, time_window));
+    self
+      .layers
+      .for_each_neutral_mut(|layer| layer.set_temporal_filter(current_time, time_window));
   }
 
   /// Update the timeline layer with time range and current interval
@@ -1023,7 +1067,7 @@ impl Map {
 
   /// Search geometries across all layers
   pub fn search_geometries(&mut self, query: &str) {
-    self.for_each_layer_mut(|layer| {
+    self.layers.for_each_neutral_mut(|layer| {
       if let Some(s) = layer.as_searchable_mut() {
         s.search_geometries(query);
       }
@@ -1033,7 +1077,8 @@ impl Map {
   /// Navigate to next search result
   pub fn next_search_result(&mut self) -> bool {
     self
-      .find_map_layer_mut(|layer| {
+      .layers
+      .find_neutral_mut(|layer| {
         let s = layer.as_searchable_mut()?;
         s.next_search_result().then_some(())
       })
@@ -1043,7 +1088,8 @@ impl Map {
   /// Navigate to previous search result
   pub fn previous_search_result(&mut self) -> bool {
     self
-      .find_map_layer_mut(|layer| {
+      .layers
+      .find_neutral_mut(|layer| {
         let s = layer.as_searchable_mut()?;
         s.previous_search_result().then_some(())
       })
@@ -1052,7 +1098,7 @@ impl Map {
 
   /// Show popup for current search result
   pub fn show_search_result_popup(&mut self) {
-    self.for_each_layer_mut(|layer| {
+    self.layers.for_each_neutral_mut(|layer| {
       if let Some(s) = layer.as_searchable_mut() {
         s.show_search_result_popup();
       }
@@ -1061,7 +1107,7 @@ impl Map {
 
   /// Filter geometries across all layers
   pub fn filter_geometries(&mut self, query: &str) {
-    self.for_each_layer_mut(|layer| {
+    self.layers.for_each_neutral_mut(|layer| {
       if let Some(s) = layer.as_searchable_mut() {
         s.filter_geometries(query);
       }
@@ -1070,7 +1116,7 @@ impl Map {
 
   /// Clear filter and show all geometries
   pub fn clear_filter(&mut self) {
-    self.for_each_layer_mut(|layer| {
+    self.layers.for_each_neutral_mut(|layer| {
       if let Some(s) = layer.as_searchable_mut() {
         s.clear_filter();
       }
@@ -1081,7 +1127,8 @@ impl Map {
   #[must_use]
   pub fn get_search_results_count(&self) -> usize {
     self
-      .find_map_layer(|layer| {
+      .layers
+      .find_neutral(|layer| {
         let count = layer.as_searchable()?.search_results_count();
         (count > 0).then_some(count)
       })
@@ -1091,7 +1138,8 @@ impl Map {
   /// Show a specific layer
   pub fn show_layer(&mut self, layer_name: &str) -> bool {
     self
-      .find_map_layer_mut(|layer| {
+      .layers
+      .find_neutral_mut(|layer| {
         (layer.name() == layer_name).then(|| {
           layer.set_visible(true);
         })
@@ -1102,7 +1150,8 @@ impl Map {
   /// Hide a specific layer
   pub fn hide_layer(&mut self, layer_name: &str) -> bool {
     self
-      .find_map_layer_mut(|layer| {
+      .layers
+      .find_neutral_mut(|layer| {
         (layer.name() == layer_name).then(|| {
           layer.set_visible(false);
         })
@@ -1113,7 +1162,8 @@ impl Map {
   /// Toggle visibility of a specific layer
   pub fn toggle_layer(&mut self, layer_name: &str) -> bool {
     self
-      .find_map_layer_mut(|layer| {
+      .layers
+      .find_neutral_mut(|layer| {
         (layer.name() == layer_name).then(|| {
           layer.set_visible(!layer.is_visible());
         })
@@ -1163,23 +1213,18 @@ pub trait MapLayerHolder {
 }
 
 struct MapLayerHolderImpl {
-  layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>>,
+  layers: MapLayerStore,
   timeline: Rc<RefCell<TimelineLayer>>,
 }
 impl MapLayerHolderImpl {
-  fn new(layers: Rc<Mutex<Vec<Box<dyn EguiLayer>>>>, timeline: Rc<RefCell<TimelineLayer>>) -> Self {
+  fn new(layers: MapLayerStore, timeline: Rc<RefCell<TimelineLayer>>) -> Self {
     Self { layers, timeline }
   }
 }
 
 impl MapLayerHolder for MapLayerHolderImpl {
   fn get_egui_layer_reader(&self) -> Box<dyn EguiLayerReader + '_> {
-    Box::new(EguiLayerReaderImpl(
-      self
-        .layers
-        .lock()
-        .unwrap_or_else(std::sync::PoisonError::into_inner),
-    ))
+    Box::new(self.layers.egui_reader())
   }
 
   fn layer_temporal_range(
@@ -1188,14 +1233,10 @@ impl MapLayerHolder for MapLayerHolderImpl {
     Option<chrono::DateTime<chrono::Utc>>,
     Option<chrono::DateTime<chrono::Utc>>,
   ) {
-    let layers = self
+    self
       .layers
-      .lock()
-      .unwrap_or_else(std::sync::PoisonError::into_inner);
-
-    layers.iter().map(|layer| layer.get_temporal_range()).fold(
-      (None, None),
-      |(earliest, latest), (layer_start, layer_end)| {
+      .fold_neutral((None, None), |(earliest, latest), layer| {
+        let (layer_start, layer_end) = layer.get_temporal_range();
         (
           match (earliest, layer_start) {
             (Some(current), Some(layer_start)) => Some(current.min(layer_start)),
@@ -1208,8 +1249,7 @@ impl MapLayerHolder for MapLayerHolderImpl {
             (current, None) => current,
           },
         )
-      },
-    )
+      })
   }
 
   fn timeline_egui_controls(&self, ui: &mut egui::Ui) {
