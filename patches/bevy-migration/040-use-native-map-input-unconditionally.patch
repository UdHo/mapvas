--- a/src/bin/mapvas/bevy_map.rs
+++ b/src/bin/mapvas/bevy_map.rs
@@ -42,7 +42,6 @@
 
 #[derive(Resource)]
 pub struct NativeMapControl {
-  enabled: bool,
   viewport: Option<MapViewport>,
   transform: Option<Transform>,
   dragging: bool,
@@ -52,7 +51,6 @@
 impl Default for NativeMapControl {
   fn default() -> Self {
     Self {
-      enabled: true,
       viewport: None,
       transform: None,
       dragging: false,
@@ -64,9 +62,6 @@
 impl NativeMapControl {
   pub fn ui(&mut self, ui: &mut egui::Ui) {
     ui.collapsing("Native Map Viewport", |ui| {
-      ui.checkbox(&mut self.enabled, "Bevy input");
-
-      ui.separator();
       if let Some(viewport) = self.viewport {
         stat_row(ui, "Zoom:", format!("{:.2}", viewport.transform.zoom));
         stat_row(
@@ -78,11 +73,6 @@
         ui.label("No viewport yet");
       }
     });
-  }
-
-  #[must_use]
-  pub fn enabled(&self) -> bool {
-    self.enabled
   }
 
   #[must_use]
@@ -107,12 +97,6 @@
   egui_wants_input: Res<EguiWantsInput>,
   time: Res<Time>,
 ) {
-  if !control.enabled {
-    control.dragging = false;
-    control.last_cursor_pos = None;
-    return;
-  }
-
   let Some(viewport) = control.viewport else {
     return;
   };
--- a/src/bin/mapvas/main.rs
+++ b/src/bin/mapvas/main.rs
@@ -98,10 +98,7 @@
     .frame(egui::Frame::NONE)
     .show(&egui_ctx, |ui| {
       if let Some(app) = state.app.as_mut() {
-        app.set_external_viewport_input(native_map.enabled());
-        if native_map.enabled()
-          && let Some(transform) = native_map.transform()
-        {
+        if let Some(transform) = native_map.transform() {
           app.set_map_transform(transform);
         }
         {
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -75,6 +75,10 @@
   fn uses_native_geometry(self) -> bool {
     self == Self::Native
   }
+
+  fn uses_external_viewport_input(self) -> bool {
+    self == Self::Native
+  }
 }
 
 impl Map {
@@ -172,7 +176,7 @@
         geometry_info: None,
         config: cfg,
         last_viewport: None,
-        external_viewport_input: false,
+        external_viewport_input: render_mode.uses_external_viewport_input(),
         render_mode,
       },
       remote,
@@ -731,10 +735,6 @@
   #[must_use]
   pub fn viewport(&self) -> Option<MapViewport> {
     self.last_viewport
-  }
-
-  pub fn set_external_viewport_input(&mut self, external_viewport_input: bool) {
-    self.external_viewport_input = external_viewport_input;
   }
 
   pub fn set_transform(&mut self, transform: Transform) {
--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -71,12 +71,6 @@
 
   pub fn set_transparent_map_background(&mut self) {
     self.transparent_map_background = true;
-  }
-
-  pub fn set_external_viewport_input(&mut self, external_viewport_input: bool) {
-    self
-      .map
-      .set_external_viewport_input(external_viewport_input);
   }
 
   pub fn set_map_transform(&mut self, transform: crate::map::coordinates::Transform) {
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -41,6 +41,7 @@
 37. `037-hide-native-attribution-in-grid-only-mode.patch`
 38. `038-remove-eframe-mapapp-adapter.patch`
 39. `039-move-eframe-to-dev-dependencies.patch`
+40. `040-use-native-map-input-unconditionally.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
