diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index eda5fbd..ed2362b 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -171,6 +171,7 @@ Apply these patches in order from the repository root:
 167. `167-hide-temporal-scan-behind-layer-holder.patch`
 168. `168-rename-egui-layer-reader.patch`
 169. `169-move-neutral-layer-trait.patch`
+170. `170-import-neutral-layer-types-directly.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/commands.rs b/src/map/mapvas_egui/layer/commands.rs
index 65b2a1c..b85ffd5 100644
--- a/src/map/mapvas_egui/layer/commands.rs
+++ b/src/map/mapvas_egui/layer/commands.rs
@@ -11,9 +11,11 @@ use crate::map::{
   coordinates::{BoundingBox, PixelCoordinate, PixelPosition, Transform},
   distance,
   geometry_collection::Geometry,
+  layer::{Layer, LayerProperties},
+  viewport::GeometrySnapshot,
 };
 
-use super::{EguiLayer, EguiMapFrame, GeometrySnapshot, Layer, LayerProperties};
+use super::{EguiLayer, EguiMapFrame};
 
 mod egui_geometry;
 mod external_cmd;
diff --git a/src/map/mapvas_egui/layer/screenshot.rs b/src/map/mapvas_egui/layer/screenshot.rs
index 295b755..5539346 100644
--- a/src/map/mapvas_egui/layer/screenshot.rs
+++ b/src/map/mapvas_egui/layer/screenshot.rs
@@ -4,9 +4,13 @@ use egui::{
   Color32, ColorImage, Context, Rect, TextureHandle, TextureOptions, UserData, ViewportCommand,
 };
 
-use crate::map::{map_event::MapEvent, mapvas_egui::helpers::current_time_screenshot_name};
+use crate::map::{
+  layer::{Layer, LayerProperties},
+  map_event::MapEvent,
+  mapvas_egui::helpers::current_time_screenshot_name,
+};
 
-use super::{EguiLayer, EguiMapFrame, Layer, LayerProperties};
+use super::{EguiLayer, EguiMapFrame};
 
 /// A layer that takes screenshots of the map, saves the image, and displays it on the map.
 pub struct ScreenshotLayer {
diff --git a/src/map/mapvas_egui/layer/shape_layer.rs b/src/map/mapvas_egui/layer/shape_layer.rs
index 3c70486..139b70d 100644
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -1,7 +1,6 @@
 use super::super::helpers::{pixel_to_pos, pos_to_pixel};
 use super::{
-  EguiLayer, EguiMapFrame, GeometrySnapshot, Layer, LayerProperties, Searchable, SubLayerInfo,
-  geometry_highlighting::GeometryHighlighter, geometry_rasterizer,
+  EguiLayer, EguiMapFrame, geometry_highlighting::GeometryHighlighter, geometry_rasterizer,
 };
 use rstar::{AABB, RTree, RTreeObject};
 
@@ -13,7 +12,9 @@ use crate::{
       tile_zoom_for_transform, tiles_in_box,
     },
     geometry_collection::{Geometry, Style},
+    layer::{Layer, LayerProperties, Searchable, SubLayerInfo},
     map_event::{Layer as EventLayer, MapEvent},
+    viewport::GeometrySnapshot,
   },
   profile_scope,
   remote::RepaintSignal,
@@ -1471,7 +1472,7 @@ mod tests {
         nested_geometry_visibility: HashMap::new(),
         recv: Arc::new(recv),
         send,
-        layer_properties: crate::map::mapvas_egui::layer::LayerProperties { visible: true },
+        layer_properties: crate::map::layer::LayerProperties { visible: true },
         geometry_highlighter: GeometryHighlighter::new(),
         config: crate::config::Config::new(),
         temporal_current_time: None,
diff --git a/src/map/mapvas_egui/layer/tile_layer.rs b/src/map/mapvas_egui/layer/tile_layer.rs
index 05e3978..f9c07ec 100644
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -13,6 +13,7 @@ use crate::{
       CANVAS_SIZE, PixelPosition, PixelRect, Tile, TileCoordinate, TilePriority, Transform,
       generate_preload_tiles, tile_zoom_for_transform, tiles_in_box,
     },
+    layer::{Layer, LayerProperties},
     tile_loader::{CachedTileLoader, TileLoader, TileSource},
     tile_renderer::{
       RasterTileRenderer, TileImage, TileRenderer, VectorTileRenderer, style_version,
@@ -22,7 +23,7 @@ use crate::{
   task_tracker::{TaskCategory, TaskGuard},
 };
 
-use super::{EguiLayer, EguiMapFrame, Layer, LayerProperties};
+use super::{EguiLayer, EguiMapFrame};
 use crate::map::mapvas_egui::helpers::pixel_to_pos;
 
 /// Compute the pixel x-coordinate of the viewport's left edge in internal canvas coordinates.
diff --git a/src/map/mapvas_egui/layer/timeline_layer.rs b/src/map/mapvas_egui/layer/timeline_layer.rs
index f9889a2..b2fe8ca 100644
--- a/src/map/mapvas_egui/layer/timeline_layer.rs
+++ b/src/map/mapvas_egui/layer/timeline_layer.rs
@@ -1,4 +1,4 @@
-use super::LayerProperties;
+use crate::map::layer::LayerProperties;
 use crate::map::mapvas_egui::timeline_widget::TimelineWidget;
 use chrono::{DateTime, Utc};
 use egui::{Rect, Ui};
