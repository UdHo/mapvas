diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -125,6 +125,7 @@ Apply these patches in order from the repository root:
 121. `121-use-pixel-position-for-native-tile-screen-space.patch`
 122. `122-use-map-viewport-pixel-accessors.patch`
 123. `123-introduce-pixel-rect.patch`
+124. `124-store-pixel-rect-in-map-viewport.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/bin/mapvas/bevy_geometry.rs b/src/bin/mapvas/bevy_geometry.rs
--- a/src/bin/mapvas/bevy_geometry.rs
+++ b/src/bin/mapvas/bevy_geometry.rs
@@ -7,7 +7,7 @@ use bevy::{
 use mapvas::{
   config::{Config, HeadingStyle},
   map::{
-    coordinates::{CANVAS_SIZE, PixelCoordinate, PixelPosition},
+    coordinates::{CANVAS_SIZE, PixelCoordinate, PixelPosition, PixelRect},
     geometry_collection::{DEFAULT_STYLE, Geometry, Style},
     viewport::{GeometrySnapshot, MapViewport},
   },
@@ -1126,14 +1126,20 @@ fn world_offset(world_x: f32, left_x: f32) -> f32 {
   ((left_x - world_x) / CANVAS_SIZE - 1e-6).ceil() * CANVAS_SIZE
 }
 
-fn segment_intersects_rect(start: PixelPosition, end: PixelPosition, rect: egui::Rect) -> bool {
-  if rect.contains(start.into()) || rect.contains(end.into()) {
+fn segment_intersects_rect(start: PixelPosition, end: PixelPosition, rect: PixelRect) -> bool {
+  if rect.contains(start) || rect.contains(end) {
     return true;
   }
 
-  let segment_rect = egui::Rect::from_min_max(
-    egui::pos2(start.x.min(end.x), start.y.min(end.y)),
-    egui::pos2(start.x.max(end.x), start.y.max(end.y)),
+  let segment_rect = PixelRect::from_min_max(
+    PixelPosition {
+      x: start.x.min(end.x),
+      y: start.y.min(end.y),
+    },
+    PixelPosition {
+      x: start.x.max(end.x),
+      y: start.y.max(end.y),
+    },
   );
   segment_rect.intersects(rect)
 }
diff --git a/src/bin/mapvas/bevy_map.rs b/src/bin/mapvas/bevy_map.rs
--- a/src/bin/mapvas/bevy_map.rs
+++ b/src/bin/mapvas/bevy_map.rs
@@ -7,7 +7,7 @@ use bevy::{
 };
 use bevy_egui::{egui, input::EguiWantsInput};
 use mapvas::map::{
-  coordinates::{CANVAS_SIZE, PixelCoordinate, PixelPosition, Transform},
+  coordinates::{CANVAS_SIZE, PixelCoordinate, PixelPosition, PixelRect, Transform},
   viewport::MapViewport,
 };
 
@@ -302,7 +302,7 @@ fn native_map_input(
   }
 
   if changed {
-    fit_to_screen(&mut transform, &viewport.rect);
+    fit_to_screen(&mut transform, viewport.rect);
     control
       .actions
       .push(NativeMapAction::TransformChanged(transform));
@@ -350,7 +350,7 @@ fn set_coordinate_to_pixel(
   transform.translate(current_pos_in_gui * (-1.0) + cursor);
 }
 
-fn fit_to_screen(transform: &mut Transform, rect: &egui::Rect) {
+fn fit_to_screen(transform: &mut Transform, rect: PixelRect) {
   transform.zoom = transform.zoom.clamp(MIN_ZOOM, MAX_ZOOM);
 
   let inv = transform.invert();
diff --git a/src/bin/mapvas/bevy_tiles.rs b/src/bin/mapvas/bevy_tiles.rs
--- a/src/bin/mapvas/bevy_tiles.rs
+++ b/src/bin/mapvas/bevy_tiles.rs
@@ -17,8 +17,8 @@ use mapvas::{
   config::{Config, TileProvider, TileType},
   map::{
     coordinates::{
-      CANVAS_SIZE, PixelPosition, Tile, TileCoordinate, TilePriority, generate_preload_tiles,
-      tile_zoom_for_transform, tiles_in_box,
+      CANVAS_SIZE, PixelPosition, PixelRect, Tile, TileCoordinate, TilePriority,
+      generate_preload_tiles, tile_zoom_for_transform, tiles_in_box,
     },
     tile_loader::{CachedTileLoader, TileLoader, TileSource},
     tile_renderer::{RasterTileRenderer, TileRenderer, VectorTileRenderer, style_version},
@@ -97,7 +97,7 @@ struct NativeTileEntry {
 }
 
 struct TileLabel {
-  background_rect: egui::Rect,
+  background_rect: PixelRect,
   background_size: Vec2,
   text_pos: PixelPosition,
   text: String,
@@ -1081,15 +1081,22 @@ fn grid_tile_color(tile: Tile) -> Color {
   }
 }
 
-fn tile_label(tile: Tile, tile_rect: egui::Rect) -> Option<TileLabel> {
+fn tile_label(tile: Tile, tile_rect: PixelRect) -> Option<TileLabel> {
   if tile_rect.width() <= TILE_LABEL_WIDTH || tile_rect.height() <= TILE_LABEL_HEIGHT {
     return None;
   }
 
   let background_size = Vec2::new(TILE_LABEL_WIDTH, TILE_LABEL_HEIGHT);
-  let background_rect = egui::Rect::from_center_size(
-    tile_rect.center(),
-    egui::vec2(TILE_LABEL_WIDTH, TILE_LABEL_HEIGHT),
+  let background_min = PixelPosition {
+    x: tile_rect.center().x - TILE_LABEL_WIDTH * 0.5,
+    y: tile_rect.center().y - TILE_LABEL_HEIGHT * 0.5,
+  };
+  let background_rect = PixelRect::from_min_size(
+    background_min,
+    PixelPosition {
+      x: TILE_LABEL_WIDTH,
+      y: TILE_LABEL_HEIGHT,
+    },
   );
   let text_pos = PixelPosition {
     x: background_rect.min.x + TILE_LABEL_TEXT_OFFSET_X,
@@ -1107,14 +1114,16 @@ fn tile_label_background_color() -> Color {
   Color::srgba(0.0, 0.0, 0.0, 180.0 / 255.0)
 }
 
-fn osm_attribution_rect(clip_rect: egui::Rect) -> egui::Rect {
-  let size = egui::vec2(OSM_ATTRIBUTION_WIDTH, OSM_ATTRIBUTION_HEIGHT);
-  egui::Rect::from_min_size(
-    egui::pos2(
-      clip_rect.max.x - size.x - OSM_ATTRIBUTION_MARGIN,
-      clip_rect.max.y - size.y - OSM_ATTRIBUTION_MARGIN,
-    ),
-    size,
+fn osm_attribution_rect(clip_rect: PixelRect) -> PixelRect {
+  PixelRect::from_min_size(
+    PixelPosition {
+      x: clip_rect.max.x - OSM_ATTRIBUTION_WIDTH - OSM_ATTRIBUTION_MARGIN,
+      y: clip_rect.max.y - OSM_ATTRIBUTION_HEIGHT - OSM_ATTRIBUTION_MARGIN,
+    },
+    PixelPosition {
+      x: OSM_ATTRIBUTION_WIDTH,
+      y: OSM_ATTRIBUTION_HEIGHT,
+    },
   )
 }
 
@@ -1142,16 +1151,16 @@ fn draw_coordinate_tile_borders(
 }
 
 fn draw_tile_border(
-  rect: egui::Rect,
+  rect: PixelRect,
   window: &Window,
   color: Color,
   gizmos: &mut Gizmos<NativeTileOverlayGizmos>,
 ) {
   let min = rect.min;
   let max = rect.max;
-  let top_left = screen_to_bevy_2d(min.into(), window);
+  let top_left = screen_to_bevy_2d(min, window);
   let top_right = screen_to_bevy_2d(PixelPosition { x: max.x, y: min.y }, window);
-  let bottom_right = screen_to_bevy_2d(max.into(), window);
+  let bottom_right = screen_to_bevy_2d(max, window);
   let bottom_left = screen_to_bevy_2d(PixelPosition { x: min.x, y: max.y }, window);
 
   gizmos.line_2d(top_left, top_right, color);
@@ -1196,11 +1205,11 @@ fn color_image_to_bevy(image: egui::ColorImage) -> Image {
   )
 }
 
-fn tile_screen_rect(viewport: MapViewport, tile: Tile) -> Option<egui::Rect> {
+fn tile_screen_rect(viewport: MapViewport, tile: Tile) -> Option<PixelRect> {
   coordinate_tile_rects(viewport, tile).into_iter().next()
 }
 
-fn coordinate_tile_rects(viewport: MapViewport, tile: Tile) -> Vec<egui::Rect> {
+fn coordinate_tile_rects(viewport: MapViewport, tile: Tile) -> Vec<PixelRect> {
   let (nw, se) = tile.position();
   let left_x = viewport_left_x(viewport);
   let dx = tile_world_offset(nw.x, left_x);
@@ -1219,7 +1228,7 @@ fn coordinate_tile_rects(viewport: MapViewport, tile: Tile) -> Vec<egui::Rect> {
       viewport.transform.apply(nw_shifted),
       viewport.transform.apply(se_shifted),
     );
-    let tile_rect = egui::Rect::from_min_max(nw_screen.into(), se_screen.into());
+    let tile_rect = PixelRect::from_min_max(nw_screen, se_screen);
     if tile_rect.intersects(viewport.rect) {
       tile_rects.push(tile_rect);
     }
@@ -1228,13 +1237,13 @@ fn coordinate_tile_rects(viewport: MapViewport, tile: Tile) -> Vec<egui::Rect> {
   tile_rects
 }
 
-fn transform_for_screen_rect(rect: egui::Rect, window: &Window, tile_zoom: u8) -> Transform {
+fn transform_for_screen_rect(rect: PixelRect, window: &Window, tile_zoom: u8) -> Transform {
   let z = -10.0 + f32::from(tile_zoom) * 0.001;
   transform_for_screen_rect_at_z(rect, window, z)
 }
 
-fn transform_for_screen_rect_at_z(rect: egui::Rect, window: &Window, z: f32) -> Transform {
-  let center = rect.center().into();
+fn transform_for_screen_rect_at_z(rect: PixelRect, window: &Window, z: f32) -> Transform {
+  let center = rect.center();
   transform_for_screen_pos(center, window, z)
 }
 
diff --git a/src/map/mapvas_egui.rs b/src/map/mapvas_egui.rs
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -402,7 +402,7 @@ impl Map {
 
   pub fn focus_all_layers(&mut self) {
     if let Some(viewport) = self.last_viewport {
-      self.show_bounding_box(viewport.rect);
+      self.show_bounding_box(viewport.egui_rect());
     }
   }
 
@@ -835,7 +835,7 @@ impl Map {
     fit_to_screen(&mut self.transform, &rect);
     self.last_viewport = Some(MapViewport {
       transform: self.transform,
-      rect,
+      rect: rect.into(),
     });
   }
 
diff --git a/src/map/viewport.rs b/src/map/viewport.rs
--- a/src/map/viewport.rs
+++ b/src/map/viewport.rs
@@ -1,14 +1,14 @@
 use egui::Rect;
 
 use super::{
-  coordinates::{PixelCoordinate, PixelPosition, Transform},
+  coordinates::{PixelCoordinate, PixelPosition, PixelRect, Transform},
   geometry_collection::Geometry,
 };
 
 #[derive(Debug, Clone, Copy)]
 pub struct MapViewport {
   pub transform: Transform,
-  pub rect: Rect,
+  pub rect: PixelRect,
 }
 
 impl MapViewport {
@@ -24,23 +24,28 @@ impl MapViewport {
 
   #[must_use]
   pub fn center(&self) -> PixelPosition {
-    self.rect.center().into()
+    self.rect.center()
   }
 
   #[must_use]
   pub fn min(&self) -> PixelPosition {
-    self.rect.min.into()
+    self.rect.min
   }
 
   #[must_use]
   pub fn max(&self) -> PixelPosition {
-    self.rect.max.into()
+    self.rect.max
   }
 
   #[must_use]
   pub fn contains(&self, pos: PixelPosition) -> bool {
-    self.rect.contains(pos.into())
+    self.rect.contains(pos)
   }
+
+  #[must_use]
+  pub fn egui_rect(&self) -> Rect {
+    self.rect.into()
+  }
 }
 
 #[derive(Clone, Default)]
