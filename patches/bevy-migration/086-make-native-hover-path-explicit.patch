--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -318,12 +318,10 @@
     }
   }
 
-  pub fn handle_hover(&mut self, pos: Option<egui::Pos2>) {
+  pub fn handle_native_hover(&mut self, pos: Option<egui::Pos2>) {
     if let Ok(mut layers) = self.layers.lock() {
       for layer in layers.iter_mut() {
-        if self.render_mode.uses_native_rendering() {
-          layer.handle_hover(pos, &self.transform);
-        }
+        layer.handle_hover(pos, &self.transform);
       }
     }
   }
--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -106,8 +106,8 @@
     self.map.open_command_context_menu(pos);
   }
 
-  pub fn handle_map_hover(&mut self, pos: Option<egui::Pos2>) {
-    self.map.handle_hover(pos);
+  pub fn handle_native_map_hover(&mut self, pos: Option<egui::Pos2>) {
+    self.map.handle_native_hover(pos);
   }
 
   pub fn handle_map_dropped_file(&mut self, path: PathBuf) {
--- a/src/bin/mapvas/main.rs
+++ b/src/bin/mapvas/main.rs
@@ -136,7 +136,7 @@
             NativeMapShortcut::Screenshot => app.request_map_screenshot(&egui_ctx),
           }
         }
-        app.handle_map_hover(native_map.hover_pos());
+        app.handle_native_map_hover(native_map.hover_pos());
         if let Some(pos) = native_map.take_double_click() {
           app.handle_map_double_click(pos);
         }
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -87,6 +87,7 @@
 83. `083-use-native-viewport-resource-for-map-input.patch`
 84. `084-remove-stored-egui-context-from-map.patch`
 85. `085-collapse-map-screenshot-target.patch`
+86. `086-make-native-hover-path-explicit.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
