diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -121,6 +121,7 @@ Apply these patches in order from the repository root:
 117. `117-use-pixel-position-for-native-actions.patch`
 118. `118-use-pixel-position-for-native-cursor-state.patch`
 119. `119-return-pixel-position-from-native-cursor-helper.patch`
+120. `120-use-pixel-position-for-native-geometry-screen-space.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/bin/mapvas/bevy_geometry.rs b/src/bin/mapvas/bevy_geometry.rs
--- a/src/bin/mapvas/bevy_geometry.rs
+++ b/src/bin/mapvas/bevy_geometry.rs
@@ -7,7 +7,7 @@ use bevy::{
 use mapvas::{
   config::{Config, HeadingStyle},
   map::{
-    coordinates::{CANVAS_SIZE, PixelCoordinate},
+    coordinates::{CANVAS_SIZE, PixelCoordinate, PixelPosition},
     geometry_collection::{DEFAULT_STYLE, Geometry, Style},
     viewport::{GeometrySnapshot, MapViewport},
   },
@@ -500,7 +500,7 @@ fn update_point_fills(
       *visibility = Visibility::Hidden;
       continue;
     };
-    if viewport.rect.contains(screen) {
+    if viewport.rect.contains(screen.into()) {
       let center = screen_to_bevy(screen, window);
       *transform = Transform::from_xyz(center.x, center.y, POINT_FILL_Z);
       *visibility = Visibility::Visible;
@@ -763,7 +763,7 @@ fn draw_geometry(
       let Some(screen) = coordinate_to_screen(*coord, viewport, left_x) else {
         return;
       };
-      if !viewport.rect.contains(screen) {
+      if !viewport.rect.contains(screen.into()) {
         return;
       }
 
@@ -795,7 +795,7 @@ fn draw_geometry(
         let Some(screen) = coordinate_to_screen(*coord, viewport, left_x) else {
           continue;
         };
-        if !viewport.rect.contains(screen) {
+        if !viewport.rect.contains(screen.into()) {
           continue;
         }
         gizmos.circle_2d(screen_to_bevy(screen, window), 1.5, color);
@@ -885,7 +885,7 @@ fn draw_highlighted_geometry(
       let Some(screen) = coordinate_to_screen(*coord, viewport, left_x) else {
         return;
       };
-      if !viewport.rect.contains(screen) {
+      if !viewport.rect.contains(screen.into()) {
         return;
       }
       gizmos.circle_2d(
@@ -1062,7 +1062,7 @@ fn coordinate_to_screen(
   coord: PixelCoordinate,
   viewport: MapViewport,
   left_x: f32,
-) -> Option<egui::Pos2> {
+) -> Option<PixelPosition> {
   if !coord.is_valid() {
     return None;
   }
@@ -1071,10 +1071,10 @@ fn coordinate_to_screen(
     x: coord.x + offset,
     y: coord.y,
   };
-  Some(viewport.transform.apply(shifted).into())
+  Some(viewport.transform.apply(shifted))
 }
 
-fn screen_to_bevy(screen: egui::Pos2, window: &Window) -> Vec2 {
+fn screen_to_bevy(screen: PixelPosition, window: &Window) -> Vec2 {
   Vec2::new(
     screen.x - window.width() / 2.0,
     window.height() / 2.0 - screen.y,
@@ -1114,15 +1114,20 @@ fn geometry_intersects_viewport(
 
 fn viewport_left_x(viewport: MapViewport) -> f32 {
   let inv = viewport.transform.invert();
-  inv.apply(egui::pos2(viewport.rect.min.x, 0.0).into()).x
+  inv
+    .apply(PixelPosition {
+      x: viewport.rect.min.x,
+      y: 0.0,
+    })
+    .x
 }
 
 fn world_offset(world_x: f32, left_x: f32) -> f32 {
   ((left_x - world_x) / CANVAS_SIZE - 1e-6).ceil() * CANVAS_SIZE
 }
 
-fn segment_intersects_rect(start: egui::Pos2, end: egui::Pos2, rect: egui::Rect) -> bool {
-  if rect.contains(start) || rect.contains(end) {
+fn segment_intersects_rect(start: PixelPosition, end: PixelPosition, rect: egui::Rect) -> bool {
+  if rect.contains(start.into()) || rect.contains(end.into()) {
     return true;
   }
 
