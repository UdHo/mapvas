--- a/src/bin/mapvas/bevy_map.rs
+++ b/src/bin/mapvas/bevy_map.rs
@@ -44,7 +44,6 @@
 
 #[derive(Resource)]
 pub struct NativeMapControl {
-  viewport: Option<MapViewport>,
   transform: Option<Transform>,
   dragging: bool,
   last_cursor_pos: Option<egui::Pos2>,
@@ -75,7 +74,6 @@
 impl Default for NativeMapControl {
   fn default() -> Self {
     Self {
-      viewport: None,
       transform: None,
       dragging: false,
       last_cursor_pos: None,
@@ -92,9 +90,9 @@
 }
 
 impl NativeMapControl {
-  pub fn ui(&mut self, ui: &mut egui::Ui) {
+  pub fn ui(&self, ui: &mut egui::Ui, viewport: Option<MapViewport>) {
     ui.collapsing("Native Map Viewport", |ui| {
-      if let Some(viewport) = self.viewport {
+      if let Some(viewport) = viewport {
         stat_row(ui, "Zoom:", format!("{:.2}", viewport.transform.zoom));
         stat_row(
           ui,
@@ -133,8 +131,7 @@
     std::mem::take(&mut self.shortcuts)
   }
 
-  pub fn set_viewport(&mut self, viewport: Option<MapViewport>) {
-    self.viewport = viewport;
+  pub fn sync_viewport(&mut self, viewport: Option<MapViewport>) {
     if let Some(viewport) = viewport {
       self.transform = Some(viewport.transform);
     } else {
@@ -145,6 +142,7 @@
 
 fn native_map_input(
   mut control: ResMut<NativeMapControl>,
+  viewport: Res<NativeMapViewport>,
   windows: Query<&Window, With<PrimaryWindow>>,
   buttons: Res<ButtonInput<MouseButton>>,
   keys: Res<ButtonInput<KeyCode>>,
@@ -152,7 +150,8 @@
   egui_wants_input: Res<EguiWantsInput>,
   time: Res<Time>,
 ) {
-  let Some(viewport) = control.viewport else {
+  let Some(viewport) = viewport.get() else {
+    control.hover_pos = None;
     return;
   };
   let Some(mut transform) = control.transform else {
--- a/src/bin/mapvas/main.rs
+++ b/src/bin/mapvas/main.rs
@@ -152,7 +152,7 @@
             geometry_version: native_geometry.geometry_version(),
           };
           let mut map_layer_controls = |ui: &mut egui::Ui| {
-            native_map.ui(ui);
+            native_map.ui(ui, native_viewport.get());
             native_tiles.ui(ui);
             native_geometry.ui(ui);
           };
@@ -174,7 +174,7 @@
       }
     });
   native_viewport.set(viewport);
-  native_map.set_viewport(viewport);
+  native_map.sync_viewport(viewport);
   if let Some(snapshot) = geometry_snapshot {
     native_geometry.update_snapshot(snapshot);
   }
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -84,6 +84,7 @@
 80. `080-route-map-event-repaint-through-signal.patch`
 81. `081-replace-native-layer-overlay-hook.patch`
 82. `082-require-repaint-for-native-map-constructor.patch`
+83. `083-use-native-viewport-resource-for-map-input.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
