diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index f7f3432..68f7bc1 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -192,6 +192,7 @@ Apply these patches in order from the repository root:
 188. `188-make-egui-mapapp-constructor-explicit.patch`
 189. `189-remove-generic-mapapp-constructor.patch`
 190. `190-make-egui-mapapp-show-explicit.patch`
+191. `191-remove-generic-mapapp-show.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/mapvas_ui.rs b/src/mapvas_ui.rs
index 6182ee1..08daee2 100644
--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -239,11 +239,6 @@ impl MapApp {
     self.show_with_map_layer_controls(ui, &mut |_| {}, None)
   }
 
-  #[allow(clippy::too_many_lines)]
-  pub fn show(&mut self, ui: &mut egui::Ui) -> MapAppOutput {
-    self.show_egui(ui)
-  }
-
   pub fn show_bevy(
     &mut self,
     ui: &mut egui::Ui,
