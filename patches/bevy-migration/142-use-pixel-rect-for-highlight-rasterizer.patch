diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 39fa541..b84fbd3 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -143,6 +143,7 @@ Apply these patches in order from the repository root:
 139. `139-use-pixel-rect-for-shape-geo-tiles.patch`
 140. `140-use-pixel-rect-for-egui-tile-selection.patch`
 141. `141-use-pixel-rect-for-geometry-rasterizer.patch`
+142. `142-use-pixel-rect-for-highlight-rasterizer.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/coordinates/coords.rs b/src/map/coordinates/coords.rs
index 789cf02..590df57 100644
--- a/src/map/coordinates/coords.rs
+++ b/src/map/coordinates/coords.rs
@@ -115,6 +115,48 @@ impl PixelRect {
     }
   }
 
+  #[must_use]
+  pub fn expand(&self, by: f32) -> Self {
+    Self {
+      min: PixelPosition {
+        x: self.min.x - by,
+        y: self.min.y - by,
+      },
+      max: PixelPosition {
+        x: self.max.x + by,
+        y: self.max.y + by,
+      },
+    }
+  }
+
+  #[must_use]
+  pub fn union(&self, other: Self) -> Self {
+    Self {
+      min: PixelPosition {
+        x: self.min.x.min(other.min.x),
+        y: self.min.y.min(other.min.y),
+      },
+      max: PixelPosition {
+        x: self.max.x.max(other.max.x),
+        y: self.max.y.max(other.max.y),
+      },
+    }
+  }
+
+  #[must_use]
+  pub fn intersect(&self, other: Self) -> Self {
+    Self {
+      min: PixelPosition {
+        x: self.min.x.max(other.min.x),
+        y: self.min.y.max(other.min.y),
+      },
+      max: PixelPosition {
+        x: self.max.x.min(other.max.x),
+        y: self.max.y.min(other.max.y),
+      },
+    }
+  }
+
   #[must_use]
   pub fn contains(&self, pos: PixelPosition) -> bool {
     self.min.x <= pos.x && pos.x <= self.max.x && self.min.y <= pos.y && pos.y <= self.max.y
diff --git a/src/map/mapvas_egui/layer/geometry_highlighting.rs b/src/map/mapvas_egui/layer/geometry_highlighting.rs
index 7319f50..27e38e9 100644
--- a/src/map/mapvas_egui/layer/geometry_highlighting.rs
+++ b/src/map/mapvas_egui/layer/geometry_highlighting.rs
@@ -1,10 +1,10 @@
 use crate::map::{
-  coordinates::{PixelCoordinate, Transform},
+  coordinates::{PixelCoordinate, PixelPosition, PixelRect, Transform},
   geometry_collection::{DEFAULT_STYLE, Geometry, Metadata},
   mapvas_egui::helpers::{color_to_color32, pixel_to_pos},
 };
 use egui::{
-  Color32, ColorImage, Painter, Pos2, Rect, Shape, Stroke,
+  Color32, ColorImage, Painter, Shape, Stroke,
   epaint::{CircleShape, PathShape, PathStroke},
 };
 use std::collections::HashMap;
@@ -205,9 +205,9 @@ fn draw_highlighted_linestring(
 pub fn rasterize_highlighted_polygons(
   geometry: &Geometry<PixelCoordinate>,
   transform: &Transform,
-  viewport: Rect,
-) -> Option<(ColorImage, Rect)> {
-  let mut bbox: Option<Rect> = None;
+  viewport: PixelRect,
+) -> Option<(ColorImage, PixelRect)> {
+  let mut bbox: Option<PixelRect> = None;
   collect_polygon_bbox(geometry, transform, &mut bbox);
   // Pad for the stroke half-width plus a feathering margin.
   let pad = HIGHLIGHT_STROKE_WIDTH * 0.5 + 1.0;
@@ -252,13 +252,13 @@ pub fn rasterize_highlighted_polygons(
 fn collect_polygon_bbox(
   geometry: &Geometry<PixelCoordinate>,
   transform: &Transform,
-  acc: &mut Option<Rect>,
+  acc: &mut Option<PixelRect>,
 ) {
   match geometry {
     Geometry::Polygon(coords, _) => {
       for c in coords {
-        let p = pixel_to_pos(transform.apply(*c));
-        let r = Rect::from_min_max(p, p);
+        let p = transform.apply(*c);
+        let r = PixelRect::from_min_max(p, p);
         *acc = Some(acc.map_or(r, |a| a.union(r)));
       }
     }
@@ -275,7 +275,7 @@ fn fill_polygons_into_pixmap(
   geometry: &Geometry<PixelCoordinate>,
   transform: &Transform,
   pixmap: &mut Pixmap,
-  origin: Pos2,
+  origin: PixelPosition,
 ) {
   match geometry {
     Geometry::Polygon(coords, metadata) => {
@@ -284,10 +284,10 @@ fn fill_polygons_into_pixmap(
       let mut iter = coords.iter();
       let Some(first) = iter.next() else { return };
       let mut pb = PathBuilder::new();
-      let p = pixel_to_pos(transform.apply(*first));
+      let p = transform.apply(*first);
       pb.move_to(p.x - origin.x, p.y - origin.y);
       for c in iter {
-        let p = pixel_to_pos(transform.apply(*c));
+        let p = transform.apply(*c);
         pb.line_to(p.x - origin.x, p.y - origin.y);
       }
       pb.close();
diff --git a/src/map/mapvas_egui/layer/shape_layer/sidebar.rs b/src/map/mapvas_egui/layer/shape_layer/sidebar.rs
index c17bfa1..01cf48c 100644
--- a/src/map/mapvas_egui/layer/shape_layer/sidebar.rs
+++ b/src/map/mapvas_egui/layer/shape_layer/sidebar.rs
@@ -3,7 +3,7 @@ use crate::map::{
   color::Color as MapColor,
   coordinates::{Coordinate, PixelCoordinate, Transform, WGS84Coordinate},
   geometry_collection::{Geometry, Metadata, Style},
-  mapvas_egui::helpers::{color_to_color32, color32_to_color},
+  mapvas_egui::helpers::{color_to_color32, color32_to_color, pixel_to_pos, rect_to_pixel},
 };
 use egui::{Color32, Rect};
 
@@ -1301,13 +1301,14 @@ impl ShapeLayer {
       return;
     };
 
+    let pixel_rect = rect_to_pixel(rect);
     let key = HighlightCacheKey {
       geometry_path: (layer_id, shape_idx, nested_path),
       viewport: [
-        rect.min.x.to_bits(),
-        rect.min.y.to_bits(),
-        rect.max.x.to_bits(),
-        rect.max.y.to_bits(),
+        pixel_rect.min.x.to_bits(),
+        pixel_rect.min.y.to_bits(),
+        pixel_rect.max.x.to_bits(),
+        pixel_rect.max.y.to_bits(),
       ],
       transform: [
         transform.zoom.to_bits(),
@@ -1320,7 +1321,9 @@ impl ShapeLayer {
     let needs_rebuild = self.highlight_texture.as_ref().is_none_or(|c| c.key != key);
     if needs_rebuild {
       use super::super::geometry_highlighting::rasterize_highlighted_polygons;
-      if let Some((image, screen_rect)) = rasterize_highlighted_polygons(&shape, transform, rect) {
+      if let Some((image, screen_rect)) =
+        rasterize_highlighted_polygons(&shape, transform, pixel_rect)
+      {
         let handle =
           ui.ctx()
             .load_texture("highlight_polygon", image, egui::TextureOptions::LINEAR);
@@ -1338,7 +1341,10 @@ impl ShapeLayer {
     if let Some(cache) = &self.highlight_texture {
       painter.image(
         cache.texture.id(),
-        cache.screen_rect,
+        Rect::from_min_max(
+          pixel_to_pos(cache.screen_rect.min),
+          pixel_to_pos(cache.screen_rect.max),
+        ),
         Rect::from_min_max(egui::pos2(0.0, 0.0), egui::pos2(1.0, 1.0)),
         Color32::WHITE,
       );
diff --git a/src/map/mapvas_egui/layer/shape_layer.rs b/src/map/mapvas_egui/layer/shape_layer.rs
index 2338e6f..6134227 100644
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -123,7 +123,7 @@ pub(super) struct HighlightCacheKey {
 pub(super) struct HighlightTextureCache {
   key: HighlightCacheKey,
   texture: egui::TextureHandle,
-  screen_rect: egui::Rect,
+  screen_rect: PixelRect,
 }
 
 mod search;
