diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 0c62456..10e25b6 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -167,6 +167,7 @@ Apply these patches in order from the repository root:
 163. `163-extract-shape-egui-overlay-helper.patch`
 164. `164-split-egui-layer-overlay-trait.patch`
 165. `165-move-egui-draw-hook-to-egui-layer.patch`
+166. `166-move-egui-controls-to-egui-layer.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/commands.rs b/src/map/mapvas_egui/layer/commands.rs
index 224c557..65b2a1c 100644
--- a/src/map/mapvas_egui/layer/commands.rs
+++ b/src/map/mapvas_egui/layer/commands.rs
@@ -290,6 +290,71 @@ impl Layer for CommandLayer {
     &mut self.layer_properties.visible
   }
 
+  fn bounding_box(&self) -> Option<BoundingBox> {
+    let bb = self
+      .commands
+      .iter()
+      .filter(|command| command.is_visible())
+      .map(|command| command.bounding_box())
+      .filter(BoundingBox::is_valid)
+      .fold(BoundingBox::default(), |acc, b| acc.extend(&b));
+
+    bb.is_valid().then_some(bb)
+  }
+
+  fn closest_geometry_with_selection(
+    &mut self,
+    pos: PixelPosition,
+    transform: &Transform,
+  ) -> Option<f64> {
+    let click_coord = transform.invert().apply(pos);
+    let tolerance_screen_pixels = 5.0; // Fixed screen pixel tolerance
+    let mut closest_distance_screen = f64::INFINITY;
+    let mut found_command: Option<usize> = None;
+
+    for (cmd_idx, command) in self.commands.iter().enumerate() {
+      if !command.is_visible() {
+        continue;
+      }
+
+      if let Some(distance_map_coords) =
+        Self::calculate_distance_to_command(&**command, click_coord)
+      {
+        // Convert map coordinate distance back to screen pixels
+        let distance_screen_pixels = distance_map_coords * f64::from(transform.zoom);
+
+        if distance_screen_pixels < closest_distance_screen
+          && distance_screen_pixels < tolerance_screen_pixels
+        {
+          closest_distance_screen = distance_screen_pixels;
+          found_command = Some(cmd_idx);
+        }
+      }
+    }
+
+    if let Some(cmd_idx) = found_command {
+      let was_different = self.highlighted_command != Some(cmd_idx);
+      self.highlighted_command = Some(cmd_idx);
+      self.just_highlighted = was_different;
+      Some(closest_distance_screen)
+    } else {
+      self.highlighted_command = None;
+      self.just_highlighted = false;
+      None
+    }
+  }
+}
+
+impl EguiLayer for CommandLayer {
+  fn draw_egui(
+    &mut self,
+    ui: &mut egui::Ui,
+    transform: &crate::map::coordinates::Transform,
+    _frame: EguiMapFrame,
+  ) {
+    self.draw_egui_geometries(ui.painter(), transform);
+  }
+
   fn ui_egui_controls(&mut self, ui: &mut egui::Ui) {
     let has_highlighted_command = self.highlighted_command.is_some();
     let layer_id = egui::Id::new("command_layer_header");
@@ -398,71 +463,6 @@ impl Layer for CommandLayer {
 
     self.just_highlighted = false;
   }
-
-  fn bounding_box(&self) -> Option<BoundingBox> {
-    let bb = self
-      .commands
-      .iter()
-      .filter(|command| command.is_visible())
-      .map(|command| command.bounding_box())
-      .filter(BoundingBox::is_valid)
-      .fold(BoundingBox::default(), |acc, b| acc.extend(&b));
-
-    bb.is_valid().then_some(bb)
-  }
-
-  fn closest_geometry_with_selection(
-    &mut self,
-    pos: PixelPosition,
-    transform: &Transform,
-  ) -> Option<f64> {
-    let click_coord = transform.invert().apply(pos);
-    let tolerance_screen_pixels = 5.0; // Fixed screen pixel tolerance
-    let mut closest_distance_screen = f64::INFINITY;
-    let mut found_command: Option<usize> = None;
-
-    for (cmd_idx, command) in self.commands.iter().enumerate() {
-      if !command.is_visible() {
-        continue;
-      }
-
-      if let Some(distance_map_coords) =
-        Self::calculate_distance_to_command(&**command, click_coord)
-      {
-        // Convert map coordinate distance back to screen pixels
-        let distance_screen_pixels = distance_map_coords * f64::from(transform.zoom);
-
-        if distance_screen_pixels < closest_distance_screen
-          && distance_screen_pixels < tolerance_screen_pixels
-        {
-          closest_distance_screen = distance_screen_pixels;
-          found_command = Some(cmd_idx);
-        }
-      }
-    }
-
-    if let Some(cmd_idx) = found_command {
-      let was_different = self.highlighted_command != Some(cmd_idx);
-      self.highlighted_command = Some(cmd_idx);
-      self.just_highlighted = was_different;
-      Some(closest_distance_screen)
-    } else {
-      self.highlighted_command = None;
-      self.just_highlighted = false;
-      None
-    }
-  }
-}
-
-impl EguiLayer for CommandLayer {
-  fn draw_egui(
-    &mut self,
-    ui: &mut egui::Ui,
-    transform: &crate::map::coordinates::Transform,
-    _frame: EguiMapFrame,
-  ) {
-    self.draw_egui_geometries(ui.painter(), transform);
-  }
 }
 
 impl CommandLayer {
diff --git a/src/map/mapvas_egui/layer/screenshot.rs b/src/map/mapvas_egui/layer/screenshot.rs
index 5e3032e..295b755 100644
--- a/src/map/mapvas_egui/layer/screenshot.rs
+++ b/src/map/mapvas_egui/layer/screenshot.rs
@@ -161,8 +161,6 @@ impl Layer for ScreenshotLayer {
   fn visible_mut(&mut self) -> &mut bool {
     &mut self.layer_properties.visible
   }
-
-  fn ui_egui_content(&mut self, _ui: &mut egui::Ui) {}
 }
 
 impl EguiLayer for ScreenshotLayer {
@@ -174,4 +172,6 @@ impl EguiLayer for ScreenshotLayer {
   ) {
     self.draw_screenshot_preview(ui, frame.rect);
   }
+
+  fn ui_egui_content(&mut self, _ui: &mut egui::Ui) {}
 }
diff --git a/src/map/mapvas_egui/layer/shape_layer.rs b/src/map/mapvas_egui/layer/shape_layer.rs
index 19f7e05..3c70486 100644
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -904,6 +904,48 @@ impl EguiLayer for ShapeLayer {
   fn draw_egui_overlay(&mut self, ui: &mut Ui, transform: &Transform) {
     self.draw_egui_overlay_content(ui, transform);
   }
+
+  fn ui_egui_controls(&mut self, ui: &mut Ui) {
+    let has_highlighted_geometry = self.geometry_highlighter.has_highlighted_geometry();
+    let layer_id = egui::Id::new("shape_layer_header");
+
+    let mut layer_header = egui::CollapsingHeader::new(self.name().to_owned())
+      .id_salt(layer_id)
+      .default_open(has_highlighted_geometry);
+
+    // Open sidebar on double-click (but not hover)
+    if self.just_double_clicked.is_some() {
+      layer_header = layer_header.open(Some(true));
+    }
+
+    layer_header.show(ui, |ui| {
+      ui.checkbox(self.visible_mut(), "visible");
+      self.ui_egui_content(ui);
+    });
+  }
+
+  fn ui_egui_content(&mut self, ui: &mut Ui) {
+    profile_scope!("ShapeLayer::ui_egui_content");
+    let has_highlighted_geometry = self.geometry_highlighter.has_highlighted_geometry();
+    let shapes_header_id = egui::Id::new("shapes_header");
+
+    let mut shapes_header = egui::CollapsingHeader::new("Shapes")
+      .id_salt(shapes_header_id)
+      .default_open(has_highlighted_geometry);
+
+    // Open sidebar on double-click (but not hover)
+    if self.just_double_clicked.is_some() {
+      shapes_header = shapes_header.open(Some(true));
+    }
+
+    shapes_header.show(ui, |ui| {
+      self.show_shape_layers(ui);
+    });
+
+    let _ = self.geometry_highlighter.was_just_highlighted();
+    // Clear double-click flag after UI update
+    self.just_double_clicked = None;
+  }
 }
 
 impl Layer for ShapeLayer {
@@ -1075,48 +1117,6 @@ impl Layer for ShapeLayer {
     &mut self.layer_properties.visible
   }
 
-  fn ui_egui_controls(&mut self, ui: &mut Ui) {
-    let has_highlighted_geometry = self.geometry_highlighter.has_highlighted_geometry();
-    let layer_id = egui::Id::new("shape_layer_header");
-
-    let mut layer_header = egui::CollapsingHeader::new(self.name().to_owned())
-      .id_salt(layer_id)
-      .default_open(has_highlighted_geometry);
-
-    // Open sidebar on double-click (but not hover)
-    if self.just_double_clicked.is_some() {
-      layer_header = layer_header.open(Some(true));
-    }
-
-    layer_header.show(ui, |ui| {
-      ui.checkbox(self.visible_mut(), "visible");
-      self.ui_egui_content(ui);
-    });
-  }
-
-  fn ui_egui_content(&mut self, ui: &mut Ui) {
-    profile_scope!("ShapeLayer::ui_egui_content");
-    let has_highlighted_geometry = self.geometry_highlighter.has_highlighted_geometry();
-    let shapes_header_id = egui::Id::new("shapes_header");
-
-    let mut shapes_header = egui::CollapsingHeader::new("Shapes")
-      .id_salt(shapes_header_id)
-      .default_open(has_highlighted_geometry);
-
-    // Open sidebar on double-click (but not hover)
-    if self.just_double_clicked.is_some() {
-      shapes_header = shapes_header.open(Some(true));
-    }
-
-    shapes_header.show(ui, |ui| {
-      self.show_shape_layers(ui);
-    });
-
-    let _ = self.geometry_highlighter.was_just_highlighted();
-    // Clear double-click flag after UI update
-    self.just_double_clicked = None;
-  }
-
   fn has_double_click_action(&self) -> bool {
     self.just_double_clicked.is_some()
   }
diff --git a/src/map/mapvas_egui/layer/tile_layer.rs b/src/map/mapvas_egui/layer/tile_layer.rs
index 9512fba..05e3978 100644
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -887,6 +887,20 @@ impl Layer for TileLayer {
     &mut self.layer_properties.visible
   }
 
+  fn has_pending_work(&self) -> bool {
+    !self.in_flight_tiles.lock().unwrap().is_empty()
+  }
+
+  fn set_headless(&mut self) {
+    self.preload_enabled = false;
+  }
+}
+
+impl EguiLayer for TileLayer {
+  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
+    self.draw_egui_tiles(ui, transform, frame);
+  }
+
   fn ui_egui_content(&mut self, ui: &mut Ui) {
     egui::ComboBox::from_label("tile provider")
       .selected_text(
@@ -982,20 +996,6 @@ impl Layer for TileLayer {
     });
   }
 
-  fn has_pending_work(&self) -> bool {
-    !self.in_flight_tiles.lock().unwrap().is_empty()
-  }
-
-  fn set_headless(&mut self) {
-    self.preload_enabled = false;
-  }
-}
-
-impl EguiLayer for TileLayer {
-  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
-    self.draw_egui_tiles(ui, transform, frame);
-  }
-
   fn draw_egui_attribution(&self, ui: &mut Ui, clip_rect: Rect) {
     if self.coordinate_display_mode != CoordinateDisplayMode::GridOnly
       && !self.loaded_tiles.is_empty()
diff --git a/src/map/mapvas_egui/layer.rs b/src/map/mapvas_egui/layer.rs
index 4673165..c3069c0 100644
--- a/src/map/mapvas_egui/layer.rs
+++ b/src/map/mapvas_egui/layer.rs
@@ -70,13 +70,6 @@ pub trait Layer {
   }
   fn process_pending_events(&mut self) {}
   fn discard_pending_events(&mut self) {}
-  fn ui_egui_controls(&mut self, ui: &mut Ui) {
-    ui.collapsing(self.name().to_owned(), |ui| {
-      ui.checkbox(self.visible_mut(), "visible");
-      self.ui_egui_content(ui);
-    });
-  }
-  fn ui_egui_content(&mut self, ui: &mut Ui);
   fn has_double_click_action(&self) -> bool {
     false
   }
@@ -197,6 +190,15 @@ pub trait EguiLayer: Layer {
 
   /// Draw egui attribution or licensing overlays after all map content is painted.
   fn draw_egui_attribution(&self, _ui: &mut Ui, _clip_rect: Rect) {}
+
+  fn ui_egui_controls(&mut self, ui: &mut Ui) {
+    ui.collapsing(self.name().to_owned(), |ui| {
+      ui.checkbox(self.visible_mut(), "visible");
+      self.ui_egui_content(ui);
+    });
+  }
+
+  fn ui_egui_content(&mut self, ui: &mut Ui);
 }
 
 /// Capability for layers that support text search and filtering of their contents.
