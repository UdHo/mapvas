diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -122,6 +122,7 @@ Apply these patches in order from the repository root:
 118. `118-use-pixel-position-for-native-cursor-state.patch`
 119. `119-return-pixel-position-from-native-cursor-helper.patch`
 120. `120-use-pixel-position-for-native-geometry-screen-space.patch`
+121. `121-use-pixel-position-for-native-tile-screen-space.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/bin/mapvas/bevy_tiles.rs b/src/bin/mapvas/bevy_tiles.rs
--- a/src/bin/mapvas/bevy_tiles.rs
+++ b/src/bin/mapvas/bevy_tiles.rs
@@ -17,7 +17,7 @@ use mapvas::{
   config::{Config, TileProvider, TileType},
   map::{
     coordinates::{
-      CANVAS_SIZE, Tile, TileCoordinate, TilePriority, Transform as MapTransform,
+      CANVAS_SIZE, PixelPosition, Tile, TileCoordinate, TilePriority, Transform as MapTransform,
       generate_preload_tiles, tile_zoom_for_transform, tiles_in_box,
     },
     tile_loader::{CachedTileLoader, TileLoader, TileSource},
@@ -99,7 +99,7 @@ struct NativeTileEntry {
 struct TileLabel {
   background_rect: egui::Rect,
   background_size: Vec2,
-  text_pos: egui::Pos2,
+  text_pos: PixelPosition,
   text: String,
 }
 
@@ -1091,8 +1091,10 @@ fn tile_label(tile: Tile, tile_rect: egui::Rect) -> Option<TileLabel> {
     tile_rect.center(),
     egui::vec2(TILE_LABEL_WIDTH, TILE_LABEL_HEIGHT),
   );
-  let text_pos =
-    background_rect.min + egui::vec2(TILE_LABEL_TEXT_OFFSET_X, TILE_LABEL_TEXT_OFFSET_Y);
+  let text_pos = PixelPosition {
+    x: background_rect.min.x + TILE_LABEL_TEXT_OFFSET_X,
+    y: background_rect.min.y + TILE_LABEL_TEXT_OFFSET_Y,
+  };
   Some(TileLabel {
     background_rect,
     background_size,
@@ -1147,10 +1149,10 @@ fn draw_tile_border(
 ) {
   let min = rect.min;
   let max = rect.max;
-  let top_left = screen_to_bevy_2d(min, window);
-  let top_right = screen_to_bevy_2d(egui::pos2(max.x, min.y), window);
-  let bottom_right = screen_to_bevy_2d(max, window);
-  let bottom_left = screen_to_bevy_2d(egui::pos2(min.x, max.y), window);
+  let top_left = screen_to_bevy_2d(min.into(), window);
+  let top_right = screen_to_bevy_2d(PixelPosition { x: max.x, y: min.y }, window);
+  let bottom_right = screen_to_bevy_2d(max.into(), window);
+  let bottom_left = screen_to_bevy_2d(PixelPosition { x: min.x, y: max.y }, window);
 
   gizmos.line_2d(top_left, top_right, color);
   gizmos.line_2d(top_right, bottom_right, color);
@@ -1232,16 +1234,16 @@ fn transform_for_screen_rect(rect: egui::Rect, window: &Window, tile_zoom: u8) -
 }
 
 fn transform_for_screen_rect_at_z(rect: egui::Rect, window: &Window, z: f32) -> Transform {
-  let center = rect.center();
+  let center = rect.center().into();
   transform_for_screen_pos(center, window, z)
 }
 
-fn transform_for_screen_pos(pos: egui::Pos2, window: &Window, z: f32) -> Transform {
+fn transform_for_screen_pos(pos: PixelPosition, window: &Window, z: f32) -> Transform {
   let bevy_pos = screen_to_bevy_2d(pos, window);
   Transform::from_xyz(bevy_pos.x, bevy_pos.y, z)
 }
 
-fn screen_to_bevy_2d(screen: egui::Pos2, window: &Window) -> Vec2 {
+fn screen_to_bevy_2d(screen: PixelPosition, window: &Window) -> Vec2 {
   Vec2::new(
     screen.x - window.width() / 2.0,
     window.height() / 2.0 - screen.y,
@@ -1262,7 +1264,12 @@ fn tile_tint(mode: CoordinateDisplayMode, tile: Tile) -> Color {
 
 fn viewport_left_x(rect: egui::Rect, transform: &MapTransform) -> f32 {
   let inv = transform.invert();
-  inv.apply(egui::pos2(rect.min.x, 0.0).into()).x
+  inv
+    .apply(PixelPosition {
+      x: rect.min.x,
+      y: 0.0,
+    })
+    .x
 }
 
 fn tile_world_offset(tile_x: f32, left_x: f32) -> f32 {
