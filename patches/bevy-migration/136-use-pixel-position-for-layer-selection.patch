diff --git a/src/map/mapvas_egui.rs b/src/map/mapvas_egui.rs
index 428fca2..4d21320 100644
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -213,7 +213,6 @@ impl Map {
 
   pub fn handle_double_click(&mut self, pos: PixelPosition) {
     log::debug!("Map: Double-click detected at {pos:?}");
-    let screen_pos = pixel_to_pos(pos);
 
     if let Ok(mut layers) = self.layers.lock() {
       log::debug!(
@@ -227,7 +226,7 @@ impl Map {
 
       // Find the closest geometry across all layers - treat all layers the same
       for (i, layer) in layers.iter_mut().enumerate() {
-        if let Some(distance) = layer.closest_geometry_with_selection(screen_pos, &self.transform) {
+        if let Some(distance) = layer.closest_geometry_with_selection(pos, &self.transform) {
           log::debug!(
             "Map: Layer '{}' can handle at distance: {:.2}",
             layer.name(),
diff --git a/src/map/mapvas_egui/layer.rs b/src/map/mapvas_egui/layer.rs
index e37eded..6b009e6 100644
--- a/src/map/mapvas_egui/layer.rs
+++ b/src/map/mapvas_egui/layer.rs
@@ -1,5 +1,5 @@
 use crate::map::coordinates::{BoundingBox, PixelPosition, Transform};
-use egui::{Pos2, Rect, Ui};
+use egui::{Rect, Ui};
 
 /// Allows to display results of commands that return coordinates.
 mod commands;
@@ -72,7 +72,11 @@ pub trait Layer {
   /// Find the closest geometry to the given position and handle selection if applicable
   /// Returns Some(distance) if this layer can handle the click, None otherwise
   /// If Some(distance) is returned, the layer should perform its selection action immediately
-  fn closest_geometry_with_selection(&mut self, _pos: Pos2, _transform: &Transform) -> Option<f64> {
+  fn closest_geometry_with_selection(
+    &mut self,
+    _pos: PixelPosition,
+    _transform: &Transform,
+  ) -> Option<f64> {
     None
   }
   /// Update the layer's configuration
diff --git a/src/map/mapvas_egui/layer/commands.rs b/src/map/mapvas_egui/layer/commands.rs
index 971e3dc..d1c19fa 100644
--- a/src/map/mapvas_egui/layer/commands.rs
+++ b/src/map/mapvas_egui/layer/commands.rs
@@ -7,11 +7,7 @@ use external_cmd::ExternalCommand;
 use log::error;
 use serde::{Deserialize, Serialize};
 
-use crate::map::{
-  coordinates::{BoundingBox, PixelCoordinate, PixelPosition, Transform},
-  mapvas_egui::helpers::pos_to_pixel,
-};
-use egui::Pos2;
+use crate::map::coordinates::{BoundingBox, PixelCoordinate, PixelPosition, Transform};
 
 use super::{GeometrySnapshot, Layer, LayerProperties, drawable::Drawable};
 
@@ -411,8 +407,12 @@ impl Layer for CommandLayer {
     bb.is_valid().then_some(bb)
   }
 
-  fn closest_geometry_with_selection(&mut self, pos: Pos2, transform: &Transform) -> Option<f64> {
-    let click_coord = transform.invert().apply(pos_to_pixel(pos));
+  fn closest_geometry_with_selection(
+    &mut self,
+    pos: PixelPosition,
+    transform: &Transform,
+  ) -> Option<f64> {
+    let click_coord = transform.invert().apply(pos);
     let tolerance_screen_pixels = 5.0; // Fixed screen pixel tolerance
     let mut closest_distance_screen = f64::INFINITY;
     let mut found_command: Option<usize> = None;
diff --git a/src/map/mapvas_egui/layer/screenshot.rs b/src/map/mapvas_egui/layer/screenshot.rs
index 69425ae..8dcde39 100644
--- a/src/map/mapvas_egui/layer/screenshot.rs
+++ b/src/map/mapvas_egui/layer/screenshot.rs
@@ -167,13 +167,5 @@ impl Layer for ScreenshotLayer {
     &mut self.layer_properties.visible
   }
 
-  fn closest_geometry_with_selection(
-    &mut self,
-    _pos: egui::Pos2,
-    _transform: &crate::map::coordinates::Transform,
-  ) -> Option<f64> {
-    None
-  }
-
   fn ui_egui_content(&mut self, _ui: &mut egui::Ui) {}
 }
diff --git a/src/map/mapvas_egui/layer/tile_layer.rs b/src/map/mapvas_egui/layer/tile_layer.rs
index 40f9832..dcb2c5d 100644
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -3,7 +3,7 @@ use std::{
   sync::{Arc, Mutex},
 };
 
-use egui::{Color32, Pos2, Rect, Ui};
+use egui::{Color32, Rect, Ui};
 use log::error;
 
 use crate::{
@@ -958,10 +958,6 @@ impl Layer for TileLayer {
     });
   }
 
-  fn closest_geometry_with_selection(&mut self, _pos: Pos2, _transform: &Transform) -> Option<f64> {
-    None
-  }
-
   fn has_pending_work(&self) -> bool {
     !self.in_flight_tiles.lock().unwrap().is_empty()
   }
diff --git a/src/map/mapvas_egui/layer/shape_layer.rs b/src/map/mapvas_egui/layer/shape_layer.rs
index 52d8176..cba00c9 100644
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -20,7 +20,7 @@ use crate::{
   task_tracker::{TaskCategory, TaskGuard},
 };
 use chrono::{DateTime, Duration, Utc};
-use egui::{Color32, ColorImage, Pos2, Rect, Ui};
+use egui::{Color32, ColorImage, Rect, Ui};
 use regex::Regex;
 use std::{
   collections::{HashMap, HashSet},
@@ -1114,8 +1114,11 @@ impl Layer for ShapeLayer {
     Some(self)
   }
 
-  fn closest_geometry_with_selection(&mut self, pos: Pos2, transform: &Transform) -> Option<f64> {
-    let click_pos = pos_to_pixel(pos);
+  fn closest_geometry_with_selection(
+    &mut self,
+    click_pos: PixelPosition,
+    transform: &Transform,
+  ) -> Option<f64> {
     let world_pos = transform.invert().apply(click_pos);
     #[allow(clippy::cast_possible_truncation)]
     let world_radius = HIGLIGHT_PIXEL_DISTANCE as f32 / transform.zoom;
@@ -1179,7 +1182,12 @@ impl Layer for ShapeLayer {
             .unwrap_or_default()
             .as_secs_f64();
           // Store click position and its world coordinate
-          self.pending_detail_popup = Some((pos, click_world_coord, detail_info, creation_time));
+          self.pending_detail_popup = Some((
+            pixel_to_pos(click_pos),
+            click_world_coord,
+            detail_info,
+            creation_time,
+          ));
         }
 
         // Handle collection expansion for double-clicks on GeometryCollections
diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 651edb8..a3c7649 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -137,6 +137,7 @@ Apply these patches in order from the repository root:
 133. `133-use-neutral-map-event-color.patch`
 134. `134-move-neutral-color-module.patch`
 135. `135-use-neutral-geometry-style-color.patch`
+136. `136-use-pixel-position-for-layer-selection.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
