diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -128,6 +128,7 @@ Apply these patches in order from the repository root:
 124. `124-store-pixel-rect-in-map-viewport.patch`
 125. `125-introduce-tile-image.patch`
 126. `126-return-tile-image-from-tile-renderers.patch`
+127. `127-keep-tile-image-egui-conversion-in-egui-layer.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/tile_layer.rs b/src/map/mapvas_egui/layer/tile_layer.rs
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -75,6 +75,10 @@ fn split_image_into_tiles(image: &TileImage, grid_size: usize) -> Vec<TileImage>
   }
 
   tiles
+}
+
+fn tile_image_to_color_image(image: TileImage) -> egui::ColorImage {
+  egui::ColorImage::from_rgba_unmultiplied(image.size, &image.rgba)
 }
 
 #[derive(Debug, Clone, Copy, PartialEq, Eq)]
@@ -667,7 +671,7 @@ impl TileLayer {
     for (tile, tile_image) in self.receiver.try_iter() {
       let handle = ui.ctx().load_texture(
         format!("{}-{}-{}", tile.zoom, tile.x, tile.y),
-        egui::ColorImage::from(tile_image),
+        tile_image_to_color_image(tile_image),
         egui::TextureOptions::default(),
       );
       self.loaded_tiles.insert(tile, handle);
diff --git a/src/map/tile_renderer.rs b/src/map/tile_renderer.rs
--- a/src/map/tile_renderer.rs
+++ b/src/map/tile_renderer.rs
@@ -8,7 +8,6 @@ pub use vector::styling::{
   style_config, style_version,
 };
 
-use egui::ColorImage;
 use thiserror::Error;
 
 use super::coordinates::Tile;
@@ -41,25 +40,6 @@ impl TileImage {
   }
 }
 
-impl From<ColorImage> for TileImage {
-  fn from(image: ColorImage) -> Self {
-    let mut rgba = Vec::with_capacity(image.pixels.len() * 4);
-    for pixel in image.pixels {
-      rgba.extend_from_slice(&pixel.to_srgba_unmultiplied());
-    }
-    Self {
-      size: image.size,
-      rgba,
-    }
-  }
-}
-
-impl From<TileImage> for ColorImage {
-  fn from(image: TileImage) -> Self {
-    ColorImage::from_rgba_unmultiplied(image.size, &image.rgba)
-  }
-}
-
 #[derive(Error, Debug)]
 pub enum TileRenderError {
   #[error("Failed to decode image: {0}")]
@@ -120,12 +100,11 @@ mod tests {
   use super::*;
 
   #[test]
-  fn tile_image_round_trips_through_egui_color_image() {
+  fn tile_image_exposes_dimensions_and_raw_rgba() {
     let source = TileImage::from_rgba_unmultiplied([2, 1], vec![255, 0, 0, 255, 0, 128, 255, 64]);
 
-    let color_image: ColorImage = source.clone().into();
-    let round_trip = TileImage::from(color_image);
-
-    assert_eq!(round_trip, source);
+    assert_eq!(source.width(), 2);
+    assert_eq!(source.height(), 1);
+    assert_eq!(source.as_raw(), [255, 0, 0, 255, 0, 128, 255, 64]);
   }
 }
