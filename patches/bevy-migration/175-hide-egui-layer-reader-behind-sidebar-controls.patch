diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index b738302..1050277 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -176,6 +176,7 @@ Apply these patches in order from the repository root:
 172. `172-add-neutral-layer-iteration-helpers.patch`
 173. `173-route-neutral-map-methods-through-layer-helpers.patch`
 174. `174-encapsulate-map-layer-store.patch`
+175. `175-hide-egui-layer-reader-behind-sidebar-controls.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui.rs b/src/map/mapvas_egui.rs
index 813db93..f5e612f 100644
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -3,7 +3,7 @@ use std::{
   path::PathBuf,
   rc::Rc,
   sync::{
-    Arc, Mutex, MutexGuard, RwLock,
+    Arc, Mutex, RwLock,
     mpsc::{Receiver, Sender},
   },
 };
@@ -122,13 +122,14 @@ impl MapLayerStore {
     f(layers.as_mut_slice());
   }
 
-  fn egui_reader(&self) -> EguiLayerReaderImpl<'_> {
-    EguiLayerReaderImpl(
-      self
-        .layers
-        .lock()
-        .unwrap_or_else(std::sync::PoisonError::into_inner),
-    )
+  fn layer_egui_controls(&self, ui: &mut egui::Ui) {
+    let mut layers = self
+      .layers
+      .lock()
+      .unwrap_or_else(std::sync::PoisonError::into_inner);
+    for layer in layers.iter_mut() {
+      layer.ui_egui_controls(ui);
+    }
   }
 }
 
@@ -1188,12 +1189,9 @@ impl Map {
   }
 }
 
-pub trait EguiLayerReader {
-  fn get_layers(&mut self) -> Box<dyn Iterator<Item = &mut Box<dyn EguiLayer>> + '_>;
-}
-
 pub trait MapLayerHolder {
-  fn get_egui_layer_reader(&self) -> Box<dyn EguiLayerReader + '_>;
+  /// Render egui sidebar controls for map layers.
+  fn layer_egui_controls(&self, ui: &mut egui::Ui);
   /// Read temporal range from map layers without exposing the egui layer reader.
   fn layer_temporal_range(
     &self,
@@ -1223,8 +1221,8 @@ impl MapLayerHolderImpl {
 }
 
 impl MapLayerHolder for MapLayerHolderImpl {
-  fn get_egui_layer_reader(&self) -> Box<dyn EguiLayerReader + '_> {
-    Box::new(self.layers.egui_reader())
+  fn layer_egui_controls(&self, ui: &mut egui::Ui) {
+    self.layers.layer_egui_controls(ui);
   }
 
   fn layer_temporal_range(
@@ -1265,11 +1263,3 @@ impl MapLayerHolder for MapLayerHolderImpl {
     self.timeline.borrow().get_temporal_range()
   }
 }
-
-struct EguiLayerReaderImpl<'a>(MutexGuard<'a, Vec<Box<dyn EguiLayer>>>);
-
-impl EguiLayerReader for EguiLayerReaderImpl<'_> {
-  fn get_layers(&mut self) -> Box<dyn Iterator<Item = &mut Box<dyn EguiLayer>> + '_> {
-    Box::new(self.0.iter_mut())
-  }
-}
diff --git a/src/mapvas_ui.rs b/src/mapvas_ui.rs
index f299c94..0198a61 100644
--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -1106,12 +1106,7 @@ impl Sidebar {
             .show(ui, |ui| {
               ui.vertical(|ui| {
                 map_layer_controls(ui);
-                {
-                  let mut layer_reader = self.map_content.get_egui_layer_reader();
-                  for layer in layer_reader.get_layers() {
-                    layer.ui_egui_controls(ui);
-                  }
-                }
+                self.map_content.layer_egui_controls(ui);
                 self.map_content.timeline_egui_controls(ui);
               });
             });
