--- a/src/bin/mapvas/bevy_map.rs
+++ b/src/bin/mapvas/bevy_map.rs
@@ -48,6 +48,7 @@
   transform: Option<Transform>,
   dragging: bool,
   last_cursor_pos: Option<egui::Pos2>,
+  hover_pos: Option<egui::Pos2>,
   last_left_click: Option<(egui::Pos2, f64)>,
   double_click: Option<egui::Pos2>,
   secondary_drag_start: Option<egui::Pos2>,
@@ -69,6 +70,7 @@
       transform: None,
       dragging: false,
       last_cursor_pos: None,
+      hover_pos: None,
       last_left_click: None,
       double_click: None,
       secondary_drag_start: None,
@@ -98,6 +100,11 @@
   #[must_use]
   pub fn transform(&self) -> Option<Transform> {
     self.transform
+  }
+
+  #[must_use]
+  pub fn hover_pos(&self) -> Option<egui::Pos2> {
+    self.hover_pos
   }
 
   pub fn take_double_click(&mut self) -> Option<egui::Pos2> {
@@ -116,6 +123,8 @@
     self.viewport = viewport;
     if let Some(viewport) = viewport {
       self.transform = Some(viewport.transform);
+    } else {
+      self.hover_pos = None;
     }
   }
 }
@@ -142,6 +151,11 @@
   let cursor = cursor_pos(window);
   let cursor_in_viewport = cursor.is_some_and(|pos| viewport.rect.contains(pos));
   let egui_popup_open = egui_wants_input.is_popup_open();
+  control.hover_pos = if !egui_popup_open && cursor_in_viewport {
+    cursor
+  } else {
+    None
+  };
   let mut changed = false;
 
   if egui_popup_open {
--- a/src/bin/mapvas/main.rs
+++ b/src/bin/mapvas/main.rs
@@ -101,6 +101,7 @@
         if let Some(transform) = native_map.transform() {
           app.set_map_transform(transform);
         }
+        app.handle_map_hover(native_map.hover_pos());
         if let Some(pos) = native_map.take_double_click() {
           app.handle_map_double_click(pos);
         }
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -242,6 +242,16 @@
   pub fn open_command_context_menu(&mut self, pos: egui::Pos2) {
     self.right_click = Some(point_to_coordinate(pos.into(), &self.transform));
     self.right_click_open_requested = true;
+  }
+
+  pub fn handle_hover(&mut self, pos: Option<egui::Pos2>) {
+    if let Ok(mut layers) = self.layers.lock() {
+      for layer in layers.iter_mut() {
+        if self.render_mode.uses_native_rendering() && layer.is_native_geometry_source() {
+          layer.handle_hover(pos, &self.transform);
+        }
+      }
+    }
   }
 
   fn handle_keys(&mut self, events: impl Iterator<Item = egui::Event>, rect: Rect) {
--- a/src/map/mapvas_egui/layer.rs
+++ b/src/map/mapvas_egui/layer.rs
@@ -39,6 +39,7 @@
 pub trait Layer {
   fn draw(&mut self, ui: &mut Ui, transform: &Transform, rect: Rect);
   fn draw_interaction_overlay(&mut self, _ui: &mut Ui, _transform: &Transform, _rect: Rect) {}
+  fn handle_hover(&mut self, _pos: Option<Pos2>, _transform: &Transform) {}
   fn clear(&mut self) {}
   fn name(&self) -> &str;
   fn visible(&self) -> bool;
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -381,7 +381,12 @@
     self.cache_version = self.version;
   }
 
-  fn prepare_interaction_frame(&mut self, ui: &egui::Ui, transform: &Transform) -> bool {
+  fn prepare_interaction_frame(
+    &mut self,
+    ui: &egui::Ui,
+    transform: &Transform,
+    sample_egui_hover: bool,
+  ) -> bool {
     self.current_transform = *transform;
     self.handle_new_shapes();
 
@@ -389,7 +394,7 @@
       return false;
     }
 
-    if let Some(mouse_pos) = ui.input(|i| i.pointer.hover_pos()) {
+    if sample_egui_hover && let Some(mouse_pos) = ui.input(|i| i.pointer.hover_pos()) {
       self.update_hover_highlighting(mouse_pos, transform);
     }
 
@@ -751,10 +756,28 @@
     self.headless = true;
   }
 
+  fn handle_hover(&mut self, pos: Option<Pos2>, transform: &Transform) {
+    profile_scope!("ShapeLayer::handle_hover");
+    self.current_transform = *transform;
+    self.handle_new_shapes();
+    self.sync_geometry_cache();
+
+    let Some(pos) = pos else {
+      self.geometry_highlighter.clear_highlighting();
+      return;
+    };
+    if !self.visible() {
+      self.geometry_highlighter.clear_highlighting();
+      return;
+    }
+
+    self.update_hover_highlighting(pos, transform);
+  }
+
   fn draw_interaction_overlay(&mut self, ui: &mut Ui, transform: &Transform, _rect: Rect) {
     profile_scope!("ShapeLayer::draw_interaction_overlay");
 
-    if !self.prepare_interaction_frame(ui, transform) {
+    if !self.prepare_interaction_frame(ui, transform, false) {
       return;
     }
 
@@ -765,7 +788,7 @@
   fn draw(&mut self, ui: &mut Ui, transform: &Transform, rect: Rect) {
     profile_scope!("ShapeLayer::draw");
 
-    if !self.prepare_interaction_frame(ui, transform) {
+    if !self.prepare_interaction_frame(ui, transform, true) {
       return;
     }
 
--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -98,6 +98,10 @@
 
   pub fn open_map_context_menu(&mut self, pos: egui::Pos2) {
     self.map.open_command_context_menu(pos);
+  }
+
+  pub fn handle_map_hover(&mut self, pos: Option<egui::Pos2>) {
+    self.map.handle_hover(pos);
   }
 
   #[must_use]
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -58,6 +58,7 @@
 54. `054-detect-double-clicks-in-native-map-input.patch`
 55. `055-native-command-drag-input.patch`
 56. `056-native-context-menu-trigger.patch`
+57. `057-native-hover-input.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
