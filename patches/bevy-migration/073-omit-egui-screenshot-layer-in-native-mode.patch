--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -123,8 +123,14 @@
 
     let (command, command_sender) = layer::CommandLayer::new();
 
-    let screenshot_layer = layer::ScreenshotLayer::new(ctx.clone());
-    let screenshot_layer_sender = screenshot_layer.get_sender();
+    let (screenshot_layer, screenshot_layer_sender) = if render_mode.uses_native_rendering() {
+      let (sender, _receiver) = std::sync::mpsc::channel();
+      (None, sender)
+    } else {
+      let screenshot_layer = layer::ScreenshotLayer::new(ctx.clone());
+      let screenshot_layer_sender = screenshot_layer.get_sender();
+      (Some(screenshot_layer), screenshot_layer_sender)
+    };
 
     let timeline = Rc::new(RefCell::new(layer::TimelineLayer::new()));
 
@@ -143,7 +149,9 @@
     }
     layer_vec.push(Box::new(shape_layer));
     layer_vec.push(Box::new(command));
-    layer_vec.push(Box::new(screenshot_layer));
+    if let Some(screenshot_layer) = screenshot_layer {
+      layer_vec.push(Box::new(screenshot_layer));
+    }
     let layers: Rc<Mutex<Vec<Box<dyn Layer>>>> = Rc::new(Mutex::new(layer_vec));
 
     let map_data_holder = Rc::new(MapLayerHolderImpl::new(layers.clone(), timeline.clone()));
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -74,6 +74,7 @@
 70. `070-native-bevy-screenshot-shortcut.patch`
 71. `071-native-bevy-remote-screenshot-handoff.patch`
 72. `072-route-native-screenshot-through-mapapp.patch`
+73. `073-omit-egui-screenshot-layer-in-native-mode.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
