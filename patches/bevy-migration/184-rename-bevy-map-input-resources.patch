diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 9fdaab2..6394215 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -185,6 +185,7 @@ Apply these patches in order from the repository root:
 181. `181-rename-bevy-geometry-snapshot-handoff.patch`
 182. `182-add-explicit-bevy-map-surface-methods.patch`
 183. `183-rename-bevy-repaint-screenshot-resources.patch`
+184. `184-rename-bevy-map-input-resources.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/bin/mapvas/bevy_app.rs b/src/bin/mapvas/bevy_app.rs
index 37931d9..57cd0b9 100644
--- a/src/bin/mapvas/bevy_app.rs
+++ b/src/bin/mapvas/bevy_app.rs
@@ -10,9 +10,7 @@ use tokio_metrics::RuntimeMonitor;
 
 use crate::{
   bevy_geometry::{NativeGeometryLayer, NativeGeometryPlugin},
-  bevy_map::{
-    NativeMapAction, NativeMapControl, NativeMapPlugin, NativeMapShortcut, NativeMapViewport,
-  },
+  bevy_map::{BevyMapAction, BevyMapControl, BevyMapPlugin, BevyMapShortcut, BevyMapViewport},
   bevy_repaint::{BevyRepaintPlugin, BevyRepaintRequests},
   bevy_screenshot::{BevyScreenshotPlugin, BevyScreenshotRequests},
   bevy_tiles::{NativeTileLayer, NativeTilePlugin, NativeTileRuntime},
@@ -81,40 +79,40 @@ fn build_runtime() -> tokio::runtime::Runtime {
   }
 }
 
-fn handle_native_map_action(app: &mut MapApp, action: NativeMapAction) {
+fn handle_bevy_map_action(app: &mut MapApp, action: BevyMapAction) {
   match action {
-    NativeMapAction::CommandDrag(drag) => {
+    BevyMapAction::CommandDrag(drag) => {
       app.handle_map_command_drag(drag.pos, drag.delta, drag.transform);
     }
-    NativeMapAction::ContextMenu(pos) => {
+    BevyMapAction::ContextMenu(pos) => {
       app.open_map_context_menu(pos);
     }
-    NativeMapAction::DoubleClick(pos) => {
+    BevyMapAction::DoubleClick(pos) => {
       app.handle_map_double_click(pos);
     }
-    NativeMapAction::DroppedFile(path) => {
+    BevyMapAction::DroppedFile(path) => {
       app.handle_map_dropped_file(path);
     }
-    NativeMapAction::Shortcut(shortcut) => handle_native_map_shortcut(app, shortcut),
-    NativeMapAction::TransformChanged(transform) => {
+    BevyMapAction::Shortcut(shortcut) => handle_bevy_map_shortcut(app, shortcut),
+    BevyMapAction::TransformChanged(transform) => {
       app.set_map_transform(transform);
     }
   }
 }
 
-fn handle_native_map_shortcut(app: &mut MapApp, shortcut: NativeMapShortcut) {
+fn handle_bevy_map_shortcut(app: &mut MapApp, shortcut: BevyMapShortcut) {
   match shortcut {
-    NativeMapShortcut::Clear => app.clear_map(),
-    NativeMapShortcut::FocusAll => app.focus_map(),
-    NativeMapShortcut::Paste => app.paste_map(),
-    NativeMapShortcut::Copy => app.copy_map(),
-    NativeMapShortcut::Screenshot => app.request_map_screenshot(),
+    BevyMapShortcut::Clear => app.clear_map(),
+    BevyMapShortcut::FocusAll => app.focus_map(),
+    BevyMapShortcut::Paste => app.paste_map(),
+    BevyMapShortcut::Copy => app.copy_map(),
+    BevyMapShortcut::Screenshot => app.request_map_screenshot(),
   }
 }
 
 fn apply_map_app_output(
   output: MapAppOutput,
-  native_viewport: &mut NativeMapViewport,
+  bevy_viewport: &mut BevyMapViewport,
   native_geometry: &mut NativeGeometryLayer,
   bevy_screenshots: &mut BevyScreenshotRequests,
   native_tiles: &mut NativeTileLayer,
@@ -123,7 +121,7 @@ fn apply_map_app_output(
   for path in output.screenshot_requests {
     bevy_screenshots.request_path(path);
   }
-  native_viewport.set(output.viewport);
+  bevy_viewport.set(output.viewport);
   if let Some(snapshot) = output.bevy_geometry_snapshot {
     native_geometry.update_snapshot(snapshot);
   }
@@ -139,8 +137,8 @@ fn apply_map_app_output(
 fn mapvas_ui_system(
   mut contexts: EguiContexts,
   mut state: NonSendMut<MapvasBevyState>,
-  mut native_map: ResMut<NativeMapControl>,
-  mut native_viewport: ResMut<NativeMapViewport>,
+  mut bevy_map: ResMut<BevyMapControl>,
+  mut bevy_viewport: ResMut<BevyMapViewport>,
   mut native_geometry: ResMut<NativeGeometryLayer>,
   mut bevy_screenshots: ResMut<BevyScreenshotRequests>,
   mut native_tiles: ResMut<NativeTileLayer>,
@@ -157,17 +155,17 @@ fn mapvas_ui_system(
     .frame(egui::Frame::NONE)
     .show(&egui_ctx, |ui| {
       if let Some(app) = state.app.as_mut() {
-        for action in native_map.take_actions() {
-          handle_native_map_action(app, action);
+        for action in bevy_map.take_actions() {
+          handle_bevy_map_action(app, action);
         }
-        app.handle_bevy_map_hover(native_map.hover_pos());
+        app.handle_bevy_map_hover(bevy_map.hover_pos());
         {
           let known_geometry_snapshot = KnownBevyGeometrySnapshot {
             snapshot_version: native_geometry.snapshot_version(),
             geometry_version: native_geometry.geometry_version(),
           };
           let mut map_layer_controls = |ui: &mut egui::Ui| {
-            native_map.ui(ui, native_viewport.get());
+            bevy_map.ui(ui, bevy_viewport.get());
             native_tiles.ui(ui);
             native_geometry.ui(ui);
           };
@@ -178,14 +176,14 @@ fn mapvas_ui_system(
   if let Some(output) = output {
     apply_map_app_output(
       output,
-      &mut native_viewport,
+      &mut bevy_viewport,
       &mut native_geometry,
       &mut bevy_screenshots,
       &mut native_tiles,
       &mut app_exit,
     );
   } else {
-    native_viewport.set(None);
+    bevy_viewport.set(None);
   }
   native_tiles.refresh_style_version();
 
@@ -227,7 +225,7 @@ pub(crate) fn run() {
         .disable::<LogPlugin>(),
     )
     .add_plugins(EguiPlugin::default())
-    .add_plugins(NativeMapPlugin)
+    .add_plugins(BevyMapPlugin)
     .add_plugins(BevyRepaintPlugin)
     .add_plugins(BevyScreenshotPlugin)
     .add_plugins(NativeTilePlugin)
diff --git a/src/bin/mapvas/bevy_geometry.rs b/src/bin/mapvas/bevy_geometry.rs
index 463bbad..06d720a 100644
--- a/src/bin/mapvas/bevy_geometry.rs
+++ b/src/bin/mapvas/bevy_geometry.rs
@@ -14,7 +14,7 @@ use mapvas::{
   },
 };
 
-use crate::bevy_map::NativeMapViewport;
+use crate::bevy_map::BevyMapViewport;
 
 const POINT_RADIUS: f32 = 4.0;
 const HIGHLIGHT_POINT_RADIUS: f32 = 10.0;
@@ -204,7 +204,7 @@ impl NativeGeometryLayer {
 fn draw_native_geometry(
   mut commands: Commands,
   mut layer: ResMut<NativeGeometryLayer>,
-  viewport: Res<NativeMapViewport>,
+  viewport: Res<BevyMapViewport>,
   windows: Query<&Window, With<PrimaryWindow>>,
   mut meshes: ResMut<Assets<Mesh>>,
   mut materials: ResMut<Assets<ColorMaterial>>,
diff --git a/src/bin/mapvas/bevy_map.rs b/src/bin/mapvas/bevy_map.rs
index 9ce8954..ef7636e 100644
--- a/src/bin/mapvas/bevy_map.rs
+++ b/src/bin/mapvas/bevy_map.rs
@@ -15,23 +15,23 @@ const KEY_PAN_SPEED: f32 = 500.0;
 const DOUBLE_CLICK_MAX_INTERVAL_SECS: f64 = 0.35;
 const DOUBLE_CLICK_MAX_DISTANCE: f32 = 8.0;
 
-pub struct NativeMapPlugin;
+pub struct BevyMapPlugin;
 
-impl Plugin for NativeMapPlugin {
+impl Plugin for BevyMapPlugin {
   fn build(&self, app: &mut App) {
     app
-      .init_resource::<NativeMapViewport>()
-      .init_resource::<NativeMapControl>()
-      .add_systems(Update, native_map_input);
+      .init_resource::<BevyMapViewport>()
+      .init_resource::<BevyMapControl>()
+      .add_systems(Update, bevy_map_input);
   }
 }
 
 #[derive(Resource, Default)]
-pub struct NativeMapViewport {
+pub struct BevyMapViewport {
   viewport: Option<MapViewport>,
 }
 
-impl NativeMapViewport {
+impl BevyMapViewport {
   pub fn set(&mut self, viewport: Option<MapViewport>) {
     self.viewport = viewport;
   }
@@ -43,32 +43,32 @@ impl NativeMapViewport {
 }
 
 #[derive(Resource)]
-pub struct NativeMapControl {
+pub struct BevyMapControl {
   dragging: bool,
   last_cursor_pos: Option<PixelPosition>,
   hover_pos: Option<PixelPosition>,
   last_left_click: Option<(PixelPosition, f64)>,
   secondary_drag_start: Option<PixelPosition>,
   secondary_drag_moved: bool,
-  actions: Vec<NativeMapAction>,
+  actions: Vec<BevyMapAction>,
 }
 
-pub struct NativeCommandDrag {
+pub struct BevyCommandDrag {
   pub pos: PixelPosition,
   pub delta: PixelPosition,
   pub transform: Transform,
 }
 
-pub enum NativeMapAction {
-  CommandDrag(NativeCommandDrag),
+pub enum BevyMapAction {
+  CommandDrag(BevyCommandDrag),
   ContextMenu(PixelPosition),
   DoubleClick(PixelPosition),
   DroppedFile(PathBuf),
-  Shortcut(NativeMapShortcut),
+  Shortcut(BevyMapShortcut),
   TransformChanged(Transform),
 }
 
-pub enum NativeMapShortcut {
+pub enum BevyMapShortcut {
   Clear,
   FocusAll,
   Paste,
@@ -76,7 +76,7 @@ pub enum NativeMapShortcut {
   Screenshot,
 }
 
-impl Default for NativeMapControl {
+impl Default for BevyMapControl {
   fn default() -> Self {
     Self {
       dragging: false,
@@ -90,9 +90,9 @@ impl Default for NativeMapControl {
   }
 }
 
-impl NativeMapControl {
+impl BevyMapControl {
   pub fn ui(&self, ui: &mut egui::Ui, viewport: Option<MapViewport>) {
-    ui.collapsing("Native Map Viewport", |ui| {
+    ui.collapsing("Bevy Map Viewport", |ui| {
       if let Some(viewport) = viewport {
         stat_row(ui, "Zoom:", format!("{:.2}", viewport.transform.zoom));
         stat_row(
@@ -111,14 +111,14 @@ impl NativeMapControl {
     self.hover_pos
   }
 
-  pub fn take_actions(&mut self) -> Vec<NativeMapAction> {
+  pub fn take_actions(&mut self) -> Vec<BevyMapAction> {
     std::mem::take(&mut self.actions)
   }
 }
 
-fn native_map_input(
-  mut control: ResMut<NativeMapControl>,
-  viewport: Res<NativeMapViewport>,
+fn bevy_map_input(
+  mut control: ResMut<BevyMapControl>,
+  viewport: Res<BevyMapViewport>,
   windows: Query<&Window, With<PrimaryWindow>>,
   buttons: Res<ButtonInput<MouseButton>>,
   keys: Res<ButtonInput<KeyCode>>,
@@ -131,7 +131,7 @@ fn native_map_input(
     if let FileDragAndDrop::DroppedFile { path_buf, .. } = event {
       control
         .actions
-        .push(NativeMapAction::DroppedFile(path_buf.clone()));
+        .push(BevyMapAction::DroppedFile(path_buf.clone()));
     }
   }
 
@@ -168,7 +168,7 @@ fn native_map_input(
         let interval = now - previous_time;
         let distance = pixel_distance(current, previous);
         if interval <= DOUBLE_CLICK_MAX_INTERVAL_SECS && distance <= DOUBLE_CLICK_MAX_DISTANCE {
-          control.actions.push(NativeMapAction::DoubleClick(current));
+          control.actions.push(BevyMapAction::DoubleClick(current));
           control.last_left_click = None;
         } else {
           control.last_left_click = Some((current, now));
@@ -210,7 +210,7 @@ fn native_map_input(
       control.secondary_drag_moved = true;
       control
         .actions
-        .push(NativeMapAction::CommandDrag(NativeCommandDrag {
+        .push(BevyMapAction::CommandDrag(BevyCommandDrag {
           pos: current,
           delta,
           transform,
@@ -225,7 +225,7 @@ fn native_map_input(
     {
       control
         .actions
-        .push(NativeMapAction::ContextMenu(cursor.unwrap_or(start)));
+        .push(BevyMapAction::ContextMenu(cursor.unwrap_or(start)));
     }
     control.secondary_drag_moved = false;
   } else if !buttons.pressed(MouseButton::Right) {
@@ -275,27 +275,27 @@ fn native_map_input(
     if keys.just_pressed(KeyCode::Delete) {
       control
         .actions
-        .push(NativeMapAction::Shortcut(NativeMapShortcut::Clear));
+        .push(BevyMapAction::Shortcut(BevyMapShortcut::Clear));
     }
     if keys.just_pressed(KeyCode::KeyF) {
       control
         .actions
-        .push(NativeMapAction::Shortcut(NativeMapShortcut::FocusAll));
+        .push(BevyMapAction::Shortcut(BevyMapShortcut::FocusAll));
     }
     if keys.just_pressed(KeyCode::KeyV) {
       control
         .actions
-        .push(NativeMapAction::Shortcut(NativeMapShortcut::Paste));
+        .push(BevyMapAction::Shortcut(BevyMapShortcut::Paste));
     }
     if keys.just_pressed(KeyCode::KeyC) {
       control
         .actions
-        .push(NativeMapAction::Shortcut(NativeMapShortcut::Copy));
+        .push(BevyMapAction::Shortcut(BevyMapShortcut::Copy));
     }
     if keys.just_pressed(KeyCode::KeyS) {
       control
         .actions
-        .push(NativeMapAction::Shortcut(NativeMapShortcut::Screenshot));
+        .push(BevyMapAction::Shortcut(BevyMapShortcut::Screenshot));
     }
   }
 
@@ -303,7 +303,7 @@ fn native_map_input(
     fit_to_screen(&mut transform, viewport.rect);
     control
       .actions
-      .push(NativeMapAction::TransformChanged(transform));
+      .push(BevyMapAction::TransformChanged(transform));
   }
 }
 
diff --git a/src/bin/mapvas/bevy_tiles.rs b/src/bin/mapvas/bevy_tiles.rs
index 4f8ba68..e75b2a8 100644
--- a/src/bin/mapvas/bevy_tiles.rs
+++ b/src/bin/mapvas/bevy_tiles.rs
@@ -29,7 +29,7 @@ use mapvas::{
   task_tracker::{TaskCategory, TaskGuard},
 };
 
-use crate::bevy_map::NativeMapViewport;
+use crate::bevy_map::BevyMapViewport;
 
 const MAX_PRELOAD_TILES: usize = 20;
 const TILE_LABEL_WIDTH: f32 = 100.0;
@@ -603,7 +603,7 @@ fn collect_finished_tiles(mut layer: ResMut<NativeTileLayer>, mut images: ResMut
 fn update_native_tiles(
   mut commands: Commands,
   mut layer: ResMut<NativeTileLayer>,
-  viewport: Res<NativeMapViewport>,
+  viewport: Res<BevyMapViewport>,
   runtime: Res<NativeTileRuntime>,
   windows: Query<&Window, With<PrimaryWindow>>,
   mut sprites: Query<(&mut Transform, &mut Sprite, &mut Visibility), With<NativeTileSprite>>,
