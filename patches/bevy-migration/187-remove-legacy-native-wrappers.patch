diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index f3a90f5..e7bde61 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -188,6 +188,7 @@ Apply these patches in order from the repository root:
 184. `184-rename-bevy-map-input-resources.patch`
 185. `185-rename-bevy-geometry-resources.patch`
 186. `186-rename-bevy-tile-resources.patch`
+187. `187-remove-legacy-native-wrappers.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui.rs b/src/map/mapvas_egui.rs
index 85a4955..aa061e8 100644
--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -110,14 +110,6 @@ impl Map {
     Self::new_impl(None, cfg, MapConstructionMode::Bevy, repaint)
   }
 
-  #[must_use]
-  pub fn new_native(
-    cfg: crate::config::Config,
-    repaint: RepaintSignal,
-  ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
-    Self::new_bevy(cfg, repaint)
-  }
-
   fn new_impl(
     ctx: Option<egui::Context>,
     cfg: crate::config::Config,
@@ -285,7 +277,7 @@ impl Map {
     };
 
     let mut close = false;
-    let inner = egui::Area::new(egui::Id::new("native_map_command_context_menu"))
+    let inner = egui::Area::new(egui::Id::new("bevy_map_command_context_menu"))
       .order(egui::Order::Foreground)
       .fixed_pos(screen_pos)
       .show(ctx, |ui| {
@@ -316,10 +308,6 @@ impl Map {
     });
   }
 
-  pub fn handle_native_hover(&mut self, pos: Option<PixelPosition>) {
-    self.handle_bevy_hover(pos);
-  }
-
   fn handle_keys(
     &mut self,
     ctx: &egui::Context,
@@ -826,10 +814,6 @@ impl Map {
 
     response
   }
-
-  pub fn ui_native(&mut self, ui: &mut Ui) -> Response {
-    self.ui_bevy(ui)
-  }
 }
 
 impl Map {
diff --git a/src/mapvas_ui.rs b/src/mapvas_ui.rs
index b2ae048..e964a52 100644
--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -63,8 +63,6 @@ pub struct KnownBevyGeometrySnapshot {
   pub geometry_version: u64,
 }
 
-pub type KnownNativeGeometrySnapshot = KnownBevyGeometrySnapshot;
-
 impl MapApp {
   #[allow(clippy::needless_pass_by_value)]
   pub fn new(
@@ -102,17 +100,6 @@ impl MapApp {
     )
   }
 
-  #[allow(clippy::needless_pass_by_value)]
-  pub fn new_native(
-    map: Map,
-    remote: Remote,
-    map_content: Rc<dyn MapLayerHolder>,
-    config: Config,
-    runtime_monitor: Option<RuntimeMonitor>,
-  ) -> Self {
-    Self::new_bevy(map, remote, map_content, config, runtime_monitor)
-  }
-
   fn new_with_ui_mode(
     map: Map,
     remote: Remote,
@@ -168,10 +155,6 @@ impl MapApp {
     self.map.handle_bevy_hover(pos);
   }
 
-  pub fn handle_native_map_hover(&mut self, pos: Option<crate::map::coordinates::PixelPosition>) {
-    self.handle_bevy_map_hover(pos);
-  }
-
   pub fn handle_map_dropped_file(&mut self, path: PathBuf) {
     self.map.handle_dropped_file(path);
   }
@@ -265,15 +248,6 @@ impl MapApp {
     self.show_with_map_layer_controls(ui, map_layer_controls, Some(known_geometry_snapshot))
   }
 
-  pub fn show_native(
-    &mut self,
-    ui: &mut egui::Ui,
-    map_layer_controls: &mut dyn FnMut(&mut egui::Ui),
-    known_geometry_snapshot: KnownNativeGeometrySnapshot,
-  ) -> MapAppOutput {
-    self.show_bevy(ui, map_layer_controls, known_geometry_snapshot)
-  }
-
   #[allow(clippy::too_many_lines)]
   fn show_with_map_layer_controls(
     &mut self,
