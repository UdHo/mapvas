--- a/src/map/mapvas_egui/layer.rs
+++ b/src/map/mapvas_egui/layer.rs
@@ -55,12 +55,6 @@
     });
   }
   fn ui_content(&mut self, ui: &mut Ui);
-  fn handle_double_click(&mut self, _pos: Pos2, _transform: &Transform) -> bool {
-    false
-  }
-  fn find_closest_geometry(&mut self, _pos: Pos2, _transform: &Transform) -> (f64, bool) {
-    (f64::INFINITY, false)
-  }
   fn has_highlighted_geometry(&self) -> bool {
     false
   }
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -949,10 +949,6 @@
 
   fn closest_geometry_with_selection(&mut self, _pos: Pos2, _transform: &Transform) -> Option<f64> {
     None
-  }
-
-  fn handle_double_click(&mut self, _pos: Pos2, _transform: &Transform) -> bool {
-    false
   }
 
   fn has_pending_work(&self) -> bool {
--- a/src/map/mapvas_egui/layer/shape_layer.rs
+++ b/src/map/mapvas_egui/layer/shape_layer.rs
@@ -1261,11 +1261,6 @@
       .fold(BoundingBox::default(), |acc, b| acc.extend(&b));
     bb.is_valid().then_some(bb)
   }
-
-  fn handle_double_click(&mut self, _pos: Pos2, _transform: &Transform) -> bool {
-    // This method is not used - double-click handling happens in closest_geometry_with_selection
-    false
-  }
 }
 
 impl Searchable for ShapeLayer {
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -52,6 +52,7 @@
 48. `048-remove-direct-eframe-dev-dependency.patch`
 49. `049-remove-stale-mapapp-bridge-accessors.patch`
 50. `050-remove-dead-map-geometry-info-state.patch`
+51. `051-remove-unused-layer-double-click-hooks.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
