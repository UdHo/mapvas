diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index b6c698b..eab8b1a 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -163,6 +163,7 @@ Apply these patches in order from the repository root:
 159. `159-remove-timeline-layer-trait-impl.patch`
 160. `160-extract-command-egui-draw-helper.patch`
 161. `161-extract-screenshot-egui-preview-helper.patch`
+162. `162-extract-tile-egui-draw-helper.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/tile_layer.rs b/src/map/mapvas_egui/layer/tile_layer.rs
index ac08fb4..1877e47 100644
--- a/src/map/mapvas_egui/layer/tile_layer.rs
+++ b/src/map/mapvas_egui/layer/tile_layer.rs
@@ -745,13 +745,13 @@ impl TileLayer {
   }
 }
 
-impl Layer for TileLayer {
+impl TileLayer {
   #[allow(
     clippy::cast_possible_truncation,
     clippy::cast_sign_loss,
     clippy::too_many_lines
   )]
-  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
+  fn draw_egui_tiles(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
     profile_scope!("TileLayer::draw_egui");
     let rect = frame.rect;
     let pixel_rect = frame.pixel_rect;
@@ -872,6 +872,12 @@ impl Layer for TileLayer {
       self.draw_coordinate_grid(ui, rect, pixel_rect, transform, min_pos, max_pos);
     }
   }
+}
+
+impl Layer for TileLayer {
+  fn draw_egui(&mut self, ui: &mut Ui, transform: &Transform, frame: EguiMapFrame) {
+    self.draw_egui_tiles(ui, transform, frame);
+  }
 
   fn name(&self) -> &str {
     NAME
