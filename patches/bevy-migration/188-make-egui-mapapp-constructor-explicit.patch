diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index e7bde61..a565019 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -189,6 +189,7 @@ Apply these patches in order from the repository root:
 185. `185-rename-bevy-geometry-resources.patch`
 186. `186-rename-bevy-tile-resources.patch`
 187. `187-remove-legacy-native-wrappers.patch`
+188. `188-make-egui-mapapp-constructor-explicit.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/headless.rs b/src/headless.rs
index 4d59ab3..0d1d489 100644
--- a/src/headless.rs
+++ b/src/headless.rs
@@ -96,7 +96,7 @@ impl HeadlessRenderer {
       remote.handle_map_event(event.clone());
     }
 
-    let mut app = MapApp::new(map, remote, data_holder, self.config.clone(), None);
+    let mut app = MapApp::new_egui(map, remote, data_holder, self.config.clone(), None);
     app.set_headless();
 
     #[allow(clippy::cast_precision_loss)]
diff --git a/src/mapvas_ui.rs b/src/mapvas_ui.rs
index e964a52..1bbf33e 100644
--- a/src/mapvas_ui.rs
+++ b/src/mapvas_ui.rs
@@ -65,7 +65,7 @@ pub struct KnownBevyGeometrySnapshot {
 
 impl MapApp {
   #[allow(clippy::needless_pass_by_value)]
-  pub fn new(
+  pub fn new_egui(
     map: Map,
     remote: Remote,
     map_content: Rc<dyn MapLayerHolder>,
@@ -82,6 +82,17 @@ impl MapApp {
     )
   }
 
+  #[allow(clippy::needless_pass_by_value)]
+  pub fn new(
+    map: Map,
+    remote: Remote,
+    map_content: Rc<dyn MapLayerHolder>,
+    config: Config,
+    runtime_monitor: Option<RuntimeMonitor>,
+  ) -> Self {
+    Self::new_egui(map, remote, map_content, config, runtime_monitor)
+  }
+
   #[allow(clippy::needless_pass_by_value)]
   pub fn new_bevy(
     map: Map,
diff --git a/tests/grid_snapshot_tests.rs b/tests/grid_snapshot_tests.rs
index 1bf270b..563d0ad 100644
--- a/tests/grid_snapshot_tests.rs
+++ b/tests/grid_snapshot_tests.rs
@@ -28,7 +28,7 @@ fn create_test_app_with_geojson_grid_mode(geojson_content: String) -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 fn create_test_app_grid_mode() -> MapApp {
@@ -36,7 +36,7 @@ fn create_test_app_grid_mode() -> MapApp {
   let ctx = egui::Context::default();
   let (mut map, remote, data_holder) = Map::new_egui(ctx, config.clone());
   map.set_headless();
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 #[tokio::test]
diff --git a/tests/highlight_snapshot_tests.rs b/tests/highlight_snapshot_tests.rs
index 1a8f0fe..dcdefdb 100644
--- a/tests/highlight_snapshot_tests.rs
+++ b/tests/highlight_snapshot_tests.rs
@@ -31,7 +31,7 @@ fn create_test_app_with_geometries() -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 #[tokio::test]
diff --git a/tests/popup_double_click_tests.rs b/tests/popup_double_click_tests.rs
index 62ffefc..a7c4e7e 100644
--- a/tests/popup_double_click_tests.rs
+++ b/tests/popup_double_click_tests.rs
@@ -61,7 +61,7 @@ fn create_test_app_with_nested_kml() -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 #[tokio::test]
diff --git a/tests/popup_snapshot_tests.rs b/tests/popup_snapshot_tests.rs
index 355e02e..dc3d33a 100644
--- a/tests/popup_snapshot_tests.rs
+++ b/tests/popup_snapshot_tests.rs
@@ -31,7 +31,7 @@ fn create_test_app_with_popup_kml() -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 #[tokio::test]
diff --git a/tests/temporal_snapshot_tests.rs b/tests/temporal_snapshot_tests.rs
index 430efaa..d83db5b 100644
--- a/tests/temporal_snapshot_tests.rs
+++ b/tests/temporal_snapshot_tests.rs
@@ -31,7 +31,7 @@ fn create_test_app_with_temporal_kml() -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 fn create_test_app_with_unix_epoch_kml() -> MapApp {
@@ -56,7 +56,7 @@ fn create_test_app_with_unix_epoch_kml() -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 #[tokio::test]
@@ -400,7 +400,7 @@ fn create_test_app_with_debug_range_kml() -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 fn create_test_app_with_unix_epoch_no_tz_kml() -> MapApp {
@@ -425,7 +425,7 @@ fn create_test_app_with_unix_epoch_no_tz_kml() -> MapApp {
   // Send a focus event to center the view on the geometries
   remote.handle_map_event(MapEvent::Focus);
 
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 #[tokio::test]
@@ -530,7 +530,7 @@ async fn large_file_temporal_parsing() {
   }
   remote.handle_map_event(MapEvent::Focus);
 
-  let app = MapApp::new(map, remote, data_holder, config, None);
+  let app = MapApp::new_egui(map, remote, data_holder, config, None);
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
diff --git a/tests/ui_tests.rs b/tests/ui_tests.rs
index fde06e1..3c2637d 100644
--- a/tests/ui_tests.rs
+++ b/tests/ui_tests.rs
@@ -7,7 +7,7 @@ fn create_test_app() -> MapApp {
   let config = Config::default();
   let ctx = egui::Context::default();
   let (map, remote, data_holder) = Map::new_egui(ctx, config.clone());
-  MapApp::new(map, remote, data_holder, config, None)
+  MapApp::new_egui(map, remote, data_holder, config, None)
 }
 
 #[tokio::test]
