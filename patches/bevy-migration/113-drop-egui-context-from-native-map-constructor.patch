--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -91,7 +91,7 @@
     cfg: crate::config::Config,
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
     let repaint = RepaintSignal::egui(ctx.clone());
-    Self::new_impl(ctx, cfg, MapConstructionOptions::EGUI, repaint)
+    Self::new_impl(Some(ctx), cfg, MapConstructionOptions::EGUI, repaint)
   }
 
   #[must_use]
@@ -101,7 +101,7 @@
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
     let repaint = RepaintSignal::egui(ctx.clone());
     Self::new_impl(
-      ctx,
+      Some(ctx),
       cfg,
       MapConstructionOptions::EGUI_WITHOUT_TILES,
       repaint,
@@ -110,20 +110,22 @@
 
   #[must_use]
   pub fn new_native(
-    ctx: egui::Context,
     cfg: crate::config::Config,
     repaint: RepaintSignal,
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
-    Self::new_impl(ctx, cfg, MapConstructionOptions::NATIVE, repaint)
+    Self::new_impl(None, cfg, MapConstructionOptions::NATIVE, repaint)
   }
 
   fn new_impl(
-    ctx: egui::Context,
+    ctx: Option<egui::Context>,
     cfg: crate::config::Config,
     options: MapConstructionOptions,
     repaint: RepaintSignal,
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
     let tile_layer = if options.include_egui_tiles {
+      let ctx = ctx
+        .as_ref()
+        .expect("egui tile construction requires an egui context");
       Some(layer::TileLayer::from_config(ctx.clone(), &cfg))
     } else {
       None
@@ -140,6 +142,9 @@
     let (screenshot_layer, screenshot_target) = if options.use_native_screenshots {
       (None, ScreenshotTarget::Native(Vec::new()))
     } else {
+      let ctx = ctx
+        .as_ref()
+        .expect("egui screenshot construction requires an egui context");
       let screenshot_layer = layer::ScreenshotLayer::new(ctx.clone());
       let screenshot_layer_sender = screenshot_layer.get_sender();
       (
--- a/src/bin/mapvas/bevy_app.rs
+++ b/src/bin/mapvas/bevy_app.rs
@@ -52,8 +52,7 @@
     let config = self.initial_config.clone();
     init_style_config(config.vector_style_file.as_deref());
 
-    let (map, remote, data_holder) =
-      Map::new_native(egui_ctx, config.clone(), self.repaint_signal.clone());
+    let (map, remote, data_holder) = Map::new_native(config.clone(), self.repaint_signal.clone());
     spawn_remote_runner_on_handle(self.runtime.handle().clone(), remote.clone());
 
     let app = MapApp::new_native(
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -114,6 +114,7 @@
 110. `110-rename-timeline-controls-egui.patch`
 111. `111-rename-egui-without-tiles-constructor.patch`
 112. `112-use-repaint-signal-in-shape-layer.patch`
+113. `113-drop-egui-context-from-native-map-constructor.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
