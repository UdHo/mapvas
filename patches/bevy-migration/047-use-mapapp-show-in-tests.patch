--- a/tests/ui_tests.rs
+++ b/tests/ui_tests.rs
@@ -1,4 +1,3 @@
-use eframe::App;
 use egui::accesskit::Role;
 use egui_kittest::Harness;
 use egui_kittest::kittest::Queryable;
@@ -17,8 +16,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -41,8 +39,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -59,8 +56,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -80,8 +76,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -98,8 +93,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -124,8 +118,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -145,8 +138,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -171,8 +163,7 @@
 
     let mut harness = Harness::new_ui_state(
       |ui, app: &mut MapApp| {
-        let mut frame = eframe::Frame::_new_kittest();
-        app.ui(ui, &mut frame);
+        app.show(ui);
       },
       app,
     );
@@ -191,8 +182,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
--- a/tests/grid_snapshot_tests.rs
+++ b/tests/grid_snapshot_tests.rs
@@ -1,4 +1,3 @@
-use eframe::App;
 use egui_kittest::Harness;
 use egui_kittest::kittest::Queryable;
 use mapvas::{
@@ -46,8 +45,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -90,8 +88,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -136,8 +133,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -172,8 +168,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -215,8 +210,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -265,8 +259,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
--- a/tests/highlight_snapshot_tests.rs
+++ b/tests/highlight_snapshot_tests.rs
@@ -1,4 +1,3 @@
-use eframe::App;
 use egui_kittest::Harness;
 use egui_kittest::kittest::Queryable;
 use mapvas::{
@@ -41,8 +40,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -82,8 +80,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
--- a/tests/popup_double_click_tests.rs
+++ b/tests/popup_double_click_tests.rs
@@ -1,4 +1,3 @@
-use eframe::App;
 use egui_kittest::Harness;
 use egui_kittest::kittest::Queryable;
 use mapvas::{
@@ -71,8 +70,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -113,8 +111,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -150,8 +147,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
--- a/tests/popup_snapshot_tests.rs
+++ b/tests/popup_snapshot_tests.rs
@@ -1,4 +1,3 @@
-use eframe::App;
 use egui_kittest::Harness;
 use egui_kittest::kittest::Queryable;
 use mapvas::{
@@ -41,8 +40,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -68,8 +66,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
--- a/tests/temporal_snapshot_tests.rs
+++ b/tests/temporal_snapshot_tests.rs
@@ -1,4 +1,3 @@
-use eframe::App;
 use egui_kittest::Harness;
 use egui_kittest::kittest::Queryable;
 use mapvas::{
@@ -66,8 +65,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -110,8 +108,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -143,8 +140,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -180,8 +176,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -220,8 +215,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -266,8 +260,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -320,8 +313,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -356,8 +348,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -443,8 +434,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -477,8 +467,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
@@ -545,8 +534,7 @@
 
   let mut harness = Harness::new_ui_state(
     |ui, app: &mut MapApp| {
-      let mut frame = eframe::Frame::_new_kittest();
-      app.ui(ui, &mut frame);
+      app.show(ui);
     },
     app,
   );
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -48,6 +48,7 @@
 44. `044-rename-native-render-mode-helper.patch`
 45. `045-skip-egui-timeline-in-native-mode.patch`
 46. `046-return-native-geometry-snapshot-in-frame-output.patch`
+47. `047-use-mapapp-show-in-tests.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
