diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 5e148e4..f3a90f5 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -187,6 +187,7 @@ Apply these patches in order from the repository root:
 183. `183-rename-bevy-repaint-screenshot-resources.patch`
 184. `184-rename-bevy-map-input-resources.patch`
 185. `185-rename-bevy-geometry-resources.patch`
+186. `186-rename-bevy-tile-resources.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/bin/mapvas/bevy_app.rs b/src/bin/mapvas/bevy_app.rs
index d79c8ef..1c88acd 100644
--- a/src/bin/mapvas/bevy_app.rs
+++ b/src/bin/mapvas/bevy_app.rs
@@ -13,7 +13,7 @@ use crate::{
   bevy_map::{BevyMapAction, BevyMapControl, BevyMapPlugin, BevyMapShortcut, BevyMapViewport},
   bevy_repaint::{BevyRepaintPlugin, BevyRepaintRequests},
   bevy_screenshot::{BevyScreenshotPlugin, BevyScreenshotRequests},
-  bevy_tiles::{NativeTileLayer, NativeTilePlugin, NativeTileRuntime},
+  bevy_tiles::{BevyTileLayer, BevyTilePlugin, BevyTileRuntime},
 };
 
 pub(crate) struct MapvasBevyState {
@@ -115,7 +115,7 @@ fn apply_map_app_output(
   bevy_viewport: &mut BevyMapViewport,
   bevy_geometry: &mut BevyGeometryLayer,
   bevy_screenshots: &mut BevyScreenshotRequests,
-  native_tiles: &mut NativeTileLayer,
+  bevy_tiles: &mut BevyTileLayer,
   app_exit: &mut MessageWriter<AppExit>,
 ) {
   for path in output.screenshot_requests {
@@ -126,7 +126,7 @@ fn apply_map_app_output(
     bevy_geometry.update_snapshot(snapshot);
   }
   if let Some(config) = output.config_update {
-    native_tiles.update_config(&config);
+    bevy_tiles.update_config(&config);
     bevy_geometry.update_config(&config);
   }
   if output.quit_requested {
@@ -141,7 +141,7 @@ fn mapvas_ui_system(
   mut bevy_viewport: ResMut<BevyMapViewport>,
   mut bevy_geometry: ResMut<BevyGeometryLayer>,
   mut bevy_screenshots: ResMut<BevyScreenshotRequests>,
-  mut native_tiles: ResMut<NativeTileLayer>,
+  mut bevy_tiles: ResMut<BevyTileLayer>,
   mut app_exit: MessageWriter<AppExit>,
 ) -> Result {
   let egui_ctx = contexts.ctx_mut()?.clone();
@@ -166,7 +166,7 @@ fn mapvas_ui_system(
           };
           let mut map_layer_controls = |ui: &mut egui::Ui| {
             bevy_map.ui(ui, bevy_viewport.get());
-            native_tiles.ui(ui);
+            bevy_tiles.ui(ui);
             bevy_geometry.ui(ui);
           };
           output = Some(app.show_bevy(ui, &mut map_layer_controls, known_geometry_snapshot));
@@ -179,13 +179,13 @@ fn mapvas_ui_system(
       &mut bevy_viewport,
       &mut bevy_geometry,
       &mut bevy_screenshots,
-      &mut native_tiles,
+      &mut bevy_tiles,
       &mut app_exit,
     );
   } else {
     bevy_viewport.set(None);
   }
-  native_tiles.refresh_style_version();
+  bevy_tiles.refresh_style_version();
 
   Ok(())
 }
@@ -210,8 +210,8 @@ pub(crate) fn run() {
       repaint_signal,
     ))
     .init_resource::<BevyGeometryLayer>()
-    .insert_resource(NativeTileRuntime(runtime_handle))
-    .insert_resource(NativeTileLayer::new(config))
+    .insert_resource(BevyTileRuntime(runtime_handle))
+    .insert_resource(BevyTileLayer::new(config))
     .add_plugins(
       DefaultPlugins
         .set(WindowPlugin {
@@ -228,7 +228,7 @@ pub(crate) fn run() {
     .add_plugins(BevyMapPlugin)
     .add_plugins(BevyRepaintPlugin)
     .add_plugins(BevyScreenshotPlugin)
-    .add_plugins(NativeTilePlugin)
+    .add_plugins(BevyTilePlugin)
     .add_plugins(BevyGeometryPlugin)
     .add_systems(Startup, setup_camera)
     .add_systems(EguiPrimaryContextPass, mapvas_ui_system)
diff --git a/src/bin/mapvas/bevy_tiles.rs b/src/bin/mapvas/bevy_tiles.rs
index e75b2a8..c6013b5 100644
--- a/src/bin/mapvas/bevy_tiles.rs
+++ b/src/bin/mapvas/bevy_tiles.rs
@@ -48,43 +48,40 @@ const OSM_ATTRIBUTION_BACKGROUND_Z: f32 = 50.0;
 const OSM_ATTRIBUTION_TEXT_Z: f32 = 51.0;
 
 #[derive(Resource)]
-pub struct NativeTileRuntime(pub tokio::runtime::Handle);
+pub struct BevyTileRuntime(pub tokio::runtime::Handle);
 
-pub struct NativeTilePlugin;
+pub struct BevyTilePlugin;
 
-impl Plugin for NativeTilePlugin {
+impl Plugin for BevyTilePlugin {
   fn build(&self, app: &mut App) {
     app
-      .init_gizmo_group::<NativeTileOverlayGizmos>()
-      .add_systems(Startup, configure_native_tile_overlay_gizmos)
-      .add_systems(
-        Update,
-        (collect_finished_tiles, update_native_tiles).chain(),
-      );
+      .init_gizmo_group::<BevyTileOverlayGizmos>()
+      .add_systems(Startup, configure_bevy_tile_overlay_gizmos)
+      .add_systems(Update, (collect_finished_tiles, update_bevy_tiles).chain());
   }
 }
 
 #[derive(Default, Reflect, GizmoConfigGroup)]
 #[reflect(Default)]
-struct NativeTileOverlayGizmos;
+struct BevyTileOverlayGizmos;
 
 #[derive(Component)]
-struct NativeTileSprite;
+struct BevyTileSprite;
 
 #[derive(Component)]
-struct NativeGridTileSprite;
+struct BevyGridTileSprite;
 
 #[derive(Component)]
-struct NativeTileLabelBackground;
+struct BevyTileLabelBackground;
 
 #[derive(Component)]
-struct NativeTileLabelText;
+struct BevyTileLabelText;
 
 #[derive(Component)]
-struct NativeOsmAttributionBackground;
+struct BevyOsmAttributionBackground;
 
 #[derive(Component)]
-struct NativeOsmAttributionText;
+struct BevyOsmAttributionText;
 
 #[derive(Clone, Copy, PartialEq, Eq)]
 enum CoordinateDisplayMode {
@@ -93,7 +90,7 @@ enum CoordinateDisplayMode {
   GridOnly,
 }
 
-struct NativeTileEntry {
+struct BevyTileEntry {
   image: Handle<Image>,
   entity: Option<Entity>,
 }
@@ -105,7 +102,7 @@ struct TileLabel {
   text: String,
 }
 
-enum NativeTileResult {
+enum BevyTileResult {
   Ready {
     generation: u64,
     tile: Tile,
@@ -118,15 +115,15 @@ enum NativeTileResult {
 }
 
 #[derive(Resource)]
-pub struct NativeTileLayer {
-  receiver: Mutex<Receiver<NativeTileResult>>,
-  sender: Sender<NativeTileResult>,
+pub struct BevyTileLayer {
+  receiver: Mutex<Receiver<BevyTileResult>>,
+  sender: Sender<BevyTileResult>,
   all_tile_loader: Vec<Arc<CachedTileLoader>>,
   tile_loader_index: usize,
   tile_providers: Vec<TileProvider>,
   tile_source: TileSource,
   visible: bool,
-  loaded_tiles: HashMap<Tile, NativeTileEntry>,
+  loaded_tiles: HashMap<Tile, BevyTileEntry>,
   in_flight_tiles: HashSet<Tile>,
   stale_entities: Vec<Entity>,
   raster_renderer: Arc<dyn TileRenderer>,
@@ -141,13 +138,13 @@ pub struct NativeTileLayer {
   coordinate_display_mode: CoordinateDisplayMode,
 }
 
-fn configure_native_tile_overlay_gizmos(mut config_store: ResMut<GizmoConfigStore>) {
-  let (config, _) = config_store.config_mut::<NativeTileOverlayGizmos>();
+fn configure_bevy_tile_overlay_gizmos(mut config_store: ResMut<GizmoConfigStore>) {
+  let (config, _) = config_store.config_mut::<BevyTileOverlayGizmos>();
   config.line.width = 1.0;
   config.depth_bias = -1.0;
 }
 
-impl NativeTileLayer {
+impl BevyTileLayer {
   #[must_use]
   pub fn new(config: Config) -> Self {
     let (sender, receiver) = std::sync::mpsc::channel();
@@ -210,7 +207,7 @@ impl NativeTileLayer {
   }
 
   pub fn ui(&mut self, ui: &mut egui::Ui) {
-    ui.collapsing("Native Tile Layer", |ui| {
+    ui.collapsing("Bevy Tile Layer", |ui| {
       ui.checkbox(&mut self.visible, "visible");
 
       let selected_provider = self
@@ -390,7 +387,7 @@ impl NativeTileLayer {
         .tile_data_with_priority(&tile, tile_source, priority)
         .await
       else {
-        let _ = sender.send(NativeTileResult::Failed { generation, tile });
+        let _ = sender.send(BevyTileResult::Failed { generation, tile });
         return;
       };
 
@@ -401,12 +398,12 @@ impl NativeTileLayer {
       let image = match render_result {
         Ok(Ok(Ok(image))) => image,
         _ => {
-          let _ = sender.send(NativeTileResult::Failed { generation, tile });
+          let _ = sender.send(BevyTileResult::Failed { generation, tile });
           return;
         }
       };
 
-      let _ = sender.send(NativeTileResult::Ready {
+      let _ = sender.send(BevyTileResult::Ready {
         generation,
         tile,
         image,
@@ -499,7 +496,7 @@ impl NativeTileLayer {
         .copied()
         .zip(split_image_into_tiles(&image, grid_size))
       {
-        let _ = sender.send(NativeTileResult::Ready {
+        let _ = sender.send(BevyTileResult::Ready {
           generation,
           tile,
           image,
@@ -525,9 +522,9 @@ impl NativeTileLayer {
   }
 }
 
-fn send_failed_tiles(sender: &Sender<NativeTileResult>, generation: u64, tiles: &[Tile]) {
+fn send_failed_tiles(sender: &Sender<BevyTileResult>, generation: u64, tiles: &[Tile]) {
   for tile in tiles {
-    let _ = sender.send(NativeTileResult::Failed {
+    let _ = sender.send(BevyTileResult::Failed {
       generation,
       tile: *tile,
     });
@@ -562,7 +559,7 @@ fn split_image_into_tiles(image: &TileImage, grid_size: usize) -> Vec<TileImage>
   tiles
 }
 
-fn collect_finished_tiles(mut layer: ResMut<NativeTileLayer>, mut images: ResMut<Assets<Image>>) {
+fn collect_finished_tiles(mut layer: ResMut<BevyTileLayer>, mut images: ResMut<Assets<Image>>) {
   let mut results = Vec::new();
   if let Ok(receiver) = layer.receiver.lock() {
     while let Ok(result) = receiver.try_recv() {
@@ -572,7 +569,7 @@ fn collect_finished_tiles(mut layer: ResMut<NativeTileLayer>, mut images: ResMut
 
   for result in results {
     match result {
-      NativeTileResult::Ready {
+      BevyTileResult::Ready {
         generation,
         tile,
         image,
@@ -584,13 +581,13 @@ fn collect_finished_tiles(mut layer: ResMut<NativeTileLayer>, mut images: ResMut
         let image = images.add(tile_image_to_bevy(image));
         layer.loaded_tiles.insert(
           tile,
-          NativeTileEntry {
+          BevyTileEntry {
             image,
             entity: None,
           },
         );
       }
-      NativeTileResult::Failed { generation, tile } => {
+      BevyTileResult::Failed { generation, tile } => {
         if generation != layer.generation {
           continue;
         }
@@ -600,58 +597,58 @@ fn collect_finished_tiles(mut layer: ResMut<NativeTileLayer>, mut images: ResMut
   }
 }
 
-fn update_native_tiles(
+fn update_bevy_tiles(
   mut commands: Commands,
-  mut layer: ResMut<NativeTileLayer>,
+  mut layer: ResMut<BevyTileLayer>,
   viewport: Res<BevyMapViewport>,
-  runtime: Res<NativeTileRuntime>,
+  runtime: Res<BevyTileRuntime>,
   windows: Query<&Window, With<PrimaryWindow>>,
-  mut sprites: Query<(&mut Transform, &mut Sprite, &mut Visibility), With<NativeTileSprite>>,
+  mut sprites: Query<(&mut Transform, &mut Sprite, &mut Visibility), With<BevyTileSprite>>,
   mut grid_sprites: Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
-    (With<NativeGridTileSprite>, Without<NativeTileSprite>),
+    (With<BevyGridTileSprite>, Without<BevyTileSprite>),
   >,
   mut label_backgrounds: Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
     (
-      With<NativeTileLabelBackground>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelText>,
+      With<BevyTileLabelBackground>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelText>,
     ),
   >,
   mut label_texts: Query<
     (&mut Transform, &mut Text2d, &mut Visibility),
     (
-      With<NativeTileLabelText>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
+      With<BevyTileLabelText>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
     ),
   >,
   mut attribution_backgrounds: Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
     (
-      With<NativeOsmAttributionBackground>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
-      Without<NativeTileLabelText>,
-      Without<NativeOsmAttributionText>,
+      With<BevyOsmAttributionBackground>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
+      Without<BevyTileLabelText>,
+      Without<BevyOsmAttributionText>,
     ),
   >,
   mut attribution_texts: Query<
     (&mut Transform, &mut Text2d, &mut Visibility),
     (
-      With<NativeOsmAttributionText>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
-      Without<NativeTileLabelText>,
-      Without<NativeOsmAttributionBackground>,
+      With<BevyOsmAttributionText>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
+      Without<BevyTileLabelText>,
+      Without<BevyOsmAttributionBackground>,
     ),
   >,
-  mut overlay_gizmos: Gizmos<NativeTileOverlayGizmos>,
+  mut overlay_gizmos: Gizmos<BevyTileOverlayGizmos>,
 ) {
   for entity in layer.stale_entities.drain(..) {
     commands.entity(entity).despawn();
@@ -794,7 +791,7 @@ fn update_native_tiles(
       let mut sprite = Sprite::from_image(entry.image.clone());
       sprite.custom_size = Some(custom_size);
       sprite.color = tint;
-      entry.entity = Some(commands.spawn((sprite, transform, NativeTileSprite)).id());
+      entry.entity = Some(commands.spawn((sprite, transform, BevyTileSprite)).id());
     }
   }
 
@@ -813,7 +810,7 @@ fn update_native_tiles(
 }
 
 fn hide_tile_sprites(
-  sprites: &mut Query<(&mut Transform, &mut Sprite, &mut Visibility), With<NativeTileSprite>>,
+  sprites: &mut Query<(&mut Transform, &mut Sprite, &mut Visibility), With<BevyTileSprite>>,
 ) {
   for (_, _, mut visibility) in sprites.iter_mut() {
     *visibility = Visibility::Hidden;
@@ -823,7 +820,7 @@ fn hide_tile_sprites(
 fn hide_grid_tile_sprites(
   sprites: &mut Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
-    (With<NativeGridTileSprite>, Without<NativeTileSprite>),
+    (With<BevyGridTileSprite>, Without<BevyTileSprite>),
   >,
 ) {
   for (_, _, mut visibility) in sprites.iter_mut() {
@@ -835,19 +832,19 @@ fn hide_tile_labels(
   backgrounds: &mut Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
     (
-      With<NativeTileLabelBackground>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelText>,
+      With<BevyTileLabelBackground>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelText>,
     ),
   >,
   texts: &mut Query<
     (&mut Transform, &mut Text2d, &mut Visibility),
     (
-      With<NativeTileLabelText>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
+      With<BevyTileLabelText>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
     ),
   >,
 ) {
@@ -863,23 +860,23 @@ fn hide_osm_attribution(
   backgrounds: &mut Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
     (
-      With<NativeOsmAttributionBackground>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
-      Without<NativeTileLabelText>,
-      Without<NativeOsmAttributionText>,
+      With<BevyOsmAttributionBackground>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
+      Without<BevyTileLabelText>,
+      Without<BevyOsmAttributionText>,
     ),
   >,
   texts: &mut Query<
     (&mut Transform, &mut Text2d, &mut Visibility),
     (
-      With<NativeOsmAttributionText>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
-      Without<NativeTileLabelText>,
-      Without<NativeOsmAttributionBackground>,
+      With<BevyOsmAttributionText>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
+      Without<BevyTileLabelText>,
+      Without<BevyOsmAttributionBackground>,
     ),
   >,
 ) {
@@ -898,7 +895,7 @@ fn update_grid_tile_sprites(
   visible_tiles: &[Tile],
   sprites: &mut Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
-    (With<NativeGridTileSprite>, Without<NativeTileSprite>),
+    (With<BevyGridTileSprite>, Without<BevyTileSprite>),
   >,
 ) {
   let mut sprite_iter = sprites.iter_mut();
@@ -919,7 +916,7 @@ fn update_grid_tile_sprites(
       commands.spawn((
         Sprite::from_color(color, custom_size),
         transform,
-        NativeGridTileSprite,
+        BevyGridTileSprite,
       ));
     }
   }
@@ -937,19 +934,19 @@ fn update_tile_labels(
   backgrounds: &mut Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
     (
-      With<NativeTileLabelBackground>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelText>,
+      With<BevyTileLabelBackground>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelText>,
     ),
   >,
   texts: &mut Query<
     (&mut Transform, &mut Text2d, &mut Visibility),
     (
-      With<NativeTileLabelText>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
+      With<BevyTileLabelText>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
     ),
   >,
 ) {
@@ -971,7 +968,7 @@ fn update_tile_labels(
         commands.spawn((
           Sprite::from_color(tile_label_background_color(), label.background_size),
           transform_for_screen_rect_at_z(label.background_rect, window, TILE_LABEL_BACKGROUND_Z),
-          NativeTileLabelBackground,
+          BevyTileLabelBackground,
         ));
       }
 
@@ -987,7 +984,7 @@ fn update_tile_labels(
           TextLayout::new_with_justify(Justify::Left),
           Anchor::TOP_LEFT,
           transform_for_screen_pos(label.text_pos, window, TILE_LABEL_TEXT_Z),
-          NativeTileLabelText,
+          BevyTileLabelText,
         ));
       }
     }
@@ -1009,23 +1006,23 @@ fn update_osm_attribution(
   backgrounds: &mut Query<
     (&mut Transform, &mut Sprite, &mut Visibility),
     (
-      With<NativeOsmAttributionBackground>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
-      Without<NativeTileLabelText>,
-      Without<NativeOsmAttributionText>,
+      With<BevyOsmAttributionBackground>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
+      Without<BevyTileLabelText>,
+      Without<BevyOsmAttributionText>,
     ),
   >,
   texts: &mut Query<
     (&mut Transform, &mut Text2d, &mut Visibility),
     (
-      With<NativeOsmAttributionText>,
-      Without<NativeTileSprite>,
-      Without<NativeGridTileSprite>,
-      Without<NativeTileLabelBackground>,
-      Without<NativeTileLabelText>,
-      Without<NativeOsmAttributionBackground>,
+      With<BevyOsmAttributionText>,
+      Without<BevyTileSprite>,
+      Without<BevyGridTileSprite>,
+      Without<BevyTileLabelBackground>,
+      Without<BevyTileLabelText>,
+      Without<BevyOsmAttributionBackground>,
     ),
   >,
 ) {
@@ -1047,7 +1044,7 @@ fn update_osm_attribution(
     commands.spawn((
       Sprite::from_color(osm_attribution_background_color(), size),
       transform_for_screen_rect_at_z(rect, window, OSM_ATTRIBUTION_BACKGROUND_Z),
-      NativeOsmAttributionBackground,
+      BevyOsmAttributionBackground,
     ));
   }
   for (_, _, mut visibility) in background_iter {
@@ -1066,7 +1063,7 @@ fn update_osm_attribution(
       TextColor(Color::srgb(35.0 / 255.0, 35.0 / 255.0, 35.0 / 255.0)),
       TextLayout::new_with_justify(Justify::Center),
       transform_for_screen_rect_at_z(rect, window, OSM_ATTRIBUTION_TEXT_Z),
-      NativeOsmAttributionText,
+      BevyOsmAttributionText,
     ));
   }
   for (_, _, mut visibility) in text_iter {
@@ -1137,7 +1134,7 @@ fn draw_coordinate_tile_borders(
   window: &Window,
   visible_tiles: &[Tile],
   mode: CoordinateDisplayMode,
-  gizmos: &mut Gizmos<NativeTileOverlayGizmos>,
+  gizmos: &mut Gizmos<BevyTileOverlayGizmos>,
 ) {
   if mode == CoordinateDisplayMode::Off {
     return;
@@ -1155,7 +1152,7 @@ fn draw_tile_border(
   rect: PixelRect,
   window: &Window,
   color: Color,
-  gizmos: &mut Gizmos<NativeTileOverlayGizmos>,
+  gizmos: &mut Gizmos<BevyTileOverlayGizmos>,
 ) {
   let min = rect.min;
   let max = rect.max;
@@ -1178,7 +1175,7 @@ fn tile_border_color(mode: CoordinateDisplayMode) -> Color {
   }
 }
 
-fn loaded_tile_or_parent(layer: &NativeTileLayer, mut tile: Tile) -> Option<Tile> {
+fn loaded_tile_or_parent(layer: &BevyTileLayer, mut tile: Tile) -> Option<Tile> {
   loop {
     if layer.loaded_tiles.contains_key(&tile) {
       return Some(tile);
