--- a/src/map/mapvas_egui.rs
+++ b/src/map/mapvas_egui.rs
@@ -57,16 +57,33 @@
   config: Config,
   last_viewport: Option<MapViewport>,
   external_viewport_input: bool,
-  native_geometry_rendering: bool,
+  render_mode: MapRenderMode,
 }
 
+#[derive(Clone, Copy, PartialEq, Eq)]
+enum MapRenderMode {
+  Egui,
+  EguiWithoutTiles,
+  Native,
+}
+
+impl MapRenderMode {
+  fn uses_egui_tiles(self) -> bool {
+    self == Self::Egui
+  }
+
+  fn uses_native_geometry(self) -> bool {
+    self == Self::Native
+  }
+}
+
 impl Map {
   #[must_use]
   pub fn new(
     ctx: egui::Context,
     cfg: crate::config::Config,
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
-    Self::new_impl(ctx, cfg, false, false)
+    Self::new_impl(ctx, cfg, MapRenderMode::Egui)
   }
 
   #[must_use]
@@ -74,7 +91,7 @@
     ctx: egui::Context,
     cfg: crate::config::Config,
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
-    Self::new_impl(ctx, cfg, true, false)
+    Self::new_impl(ctx, cfg, MapRenderMode::EguiWithoutTiles)
   }
 
   #[must_use]
@@ -82,19 +99,18 @@
     ctx: egui::Context,
     cfg: crate::config::Config,
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
-    Self::new_impl(ctx, cfg, true, true)
+    Self::new_impl(ctx, cfg, MapRenderMode::Native)
   }
 
   fn new_impl(
     ctx: egui::Context,
     cfg: crate::config::Config,
-    no_tile_layer: bool,
-    native_geometry_rendering: bool,
+    render_mode: MapRenderMode,
   ) -> (Self, Remote, Rc<dyn MapLayerHolder>) {
-    let tile_layer = if no_tile_layer {
-      None
-    } else {
+    let tile_layer = if render_mode.uses_egui_tiles() {
       Some(layer::TileLayer::from_config(ctx.clone(), &cfg))
+    } else {
+      None
     };
     let shape_info = std::sync::Arc::new(std::sync::RwLock::new(std::collections::HashMap::new()));
     let shape_layer = layer::ShapeLayer::new(cfg.clone(), ctx.clone(), shape_info.clone());
@@ -157,7 +173,7 @@
         config: cfg,
         last_viewport: None,
         external_viewport_input: false,
-        native_geometry_rendering,
+        render_mode,
       },
       remote,
       map_data_holder,
@@ -624,7 +640,7 @@
       {
         for layer in layer_guard.iter_mut() {
           layer.process_pending_events();
-          if self.native_geometry_rendering && layer.is_native_geometry_source() {
+          if self.render_mode.uses_native_geometry() && layer.is_native_geometry_source() {
             profile_scope!("Layer::draw_interaction_overlay", layer.name());
             layer.draw_interaction_overlay(ui, &self.transform, rect);
             continue;
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -37,6 +37,7 @@
 33. `033-native-osm-attribution-overlay.patch`
 34. `034-native-rendering-map-constructor.patch`
 35. `035-remove-native-rendering-setter.patch`
+36. `036-use-explicit-map-render-mode.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
