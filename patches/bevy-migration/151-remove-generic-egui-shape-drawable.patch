diff --git a/patches/bevy-migration/README.md b/patches/bevy-migration/README.md
index 6e8d06c..e424228 100644
--- a/patches/bevy-migration/README.md
+++ b/patches/bevy-migration/README.md
@@ -152,6 +152,7 @@ Apply these patches in order from the repository root:
 148. `148-use-geometry-command-results.patch`
 149. `149-trim-drawable-to-egui-drawing.patch`
 150. `150-rename-egui-drawable-abstraction.patch`
+151. `151-remove-generic-egui-shape-drawable.patch`
 
 Each step was formatted with `cargo fmt --all` and verified with `cargo c`.
 The current environment cannot write `.git/index.lock`, so these patches replace the requested commits.
diff --git a/src/map/mapvas_egui/layer/egui_drawable.rs b/src/map/mapvas_egui/layer/egui_drawable.rs
index d6c64fb..778366c 100644
--- a/src/map/mapvas_egui/layer/egui_drawable.rs
+++ b/src/map/mapvas_egui/layer/egui_drawable.rs
@@ -32,12 +32,6 @@ pub trait EguiDrawable {
   }
 }
 
-impl EguiDrawable for Shape {
-  fn draw(&self, painter: &Painter, _transform: &Transform) {
-    painter.add(self.clone());
-  }
-}
-
 impl<C: Coordinate + 'static> EguiDrawable for Geometry<C> {
   fn draw(&self, painter: &Painter, transform: &Transform) {
     self.draw_with_style(painter, transform, HeadingStyle::default());
